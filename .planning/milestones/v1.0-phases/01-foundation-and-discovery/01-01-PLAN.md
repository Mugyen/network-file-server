---
phase: 01-foundation-and-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/__init__.py
  - server/app/main.py
  - server/app/config.py
  - server/app/exceptions.py
  - server/app/models/__init__.py
  - server/app/models/schemas.py
  - server/app/models/enums.py
  - server/app/routers/__init__.py
  - server/app/routers/files.py
  - server/app/services/__init__.py
  - server/app/services/file_service.py
  - server/tests/__init__.py
  - server/tests/conftest.py
  - server/tests/test_file_service.py
  - server/tests/test_routes_files.py
  - server/tests/test_config.py
  - server/tests/test_cors.py
  - pyproject.toml
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "FastAPI app starts and responds to HTTP requests"
    - "CLI accepts a folder path argument and optional port"
    - "Requesting paths containing ../ or absolute paths outside the shared folder returns 403"
    - "GET /api/files returns JSON listing of files in the shared folder with name, size, type, and modified date"
    - "API responses include CORS headers allowing cross-origin requests"
  artifacts:
    - path: "server/app/main.py"
      provides: "FastAPI application factory with CORS middleware and router mounting"
      exports: ["create_app", "app"]
    - path: "server/app/config.py"
      provides: "ServerConfig with shared_folder and port from CLI args"
      contains: "class ServerConfig"
    - path: "server/app/services/file_service.py"
      provides: "Path traversal guard and directory listing"
      exports: ["resolve_safe_path", "list_directory"]
    - path: "server/app/models/schemas.py"
      provides: "Pydantic response models"
      exports: ["FileEntry", "DirectoryListing"]
    - path: "server/app/routers/files.py"
      provides: "File listing API endpoint"
      contains: "router"
    - path: "server/app/exceptions.py"
      provides: "Typed exceptions for path traversal and file errors"
      exports: ["PathTraversalError"]
  key_links:
    - from: "server/app/routers/files.py"
      to: "server/app/services/file_service.py"
      via: "import list_directory"
      pattern: "from app\\.services\\.file_service import"
    - from: "server/app/services/file_service.py"
      to: "server/app/config.py"
      via: "reads shared_folder from config"
      pattern: "config\\.shared_folder"
    - from: "server/app/main.py"
      to: "server/app/routers/files.py"
      via: "include_router"
      pattern: "app\\.include_router"
---

<objective>
Build the FastAPI backend foundation: project scaffolding, CLI configuration, path traversal protection, file listing API, and CORS middleware.

Purpose: Establishes the core backend that all subsequent plans build on -- the app factory, service layer, and the critical security boundary (path traversal guard).
Output: Working FastAPI server that accepts a folder path via CLI, lists directory contents via REST API, blocks path traversal attacks, and allows cross-origin requests.
</objective>

<execution_context>
@/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-discovery/01-RESEARCH.md

Reference the existing codebase for CLI arg patterns:
@wifi_file_server.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies, create project structure, and build config + path safety service</name>
  <files>
    pyproject.toml,
    server/app/__init__.py,
    server/app/config.py,
    server/app/exceptions.py,
    server/app/models/__init__.py,
    server/app/models/schemas.py,
    server/app/models/enums.py,
    server/app/services/__init__.py,
    server/app/services/file_service.py,
    server/tests/__init__.py,
    server/tests/conftest.py,
    server/tests/test_file_service.py,
    server/tests/test_config.py
  </files>
  <behavior>
    - resolve_safe_path("subdir/file.txt") with base_dir containing that file returns the resolved Path
    - resolve_safe_path("../etc/passwd") raises PathTraversalError
    - resolve_safe_path("subdir/../../etc/passwd") raises PathTraversalError
    - resolve_safe_path("/etc/passwd") raises PathTraversalError (absolute path)
    - resolve_safe_path("valid/../valid/file.txt") returns the resolved path (stays within base)
    - resolve_safe_path with symlink pointing outside base raises PathTraversalError
    - resolve_safe_path("nonexistent.txt") raises FileNotFoundError
    - list_directory(base, "") returns DirectoryListing with correct entries for files and subdirectories
    - list_directory entries include name, size (int), size_display (human readable), type (FileType enum), modified (ISO string)
    - list_directory(base, "subdir") lists contents of a subdirectory
    - list_directory(base, "../") raises PathTraversalError
    - ServerConfig created from argparse namespace with folder and port
    - ServerConfig validates folder exists and is a directory, raises ValueError otherwise
  </behavior>
  <action>
    1. Update pyproject.toml: add FastAPI, uvicorn[standard], pydantic, python-multipart, aiofiles, qrcode, ifaddr as dependencies. Add pytest, pytest-asyncio, httpx, ruff as dev dependencies. Add [tool.pytest.ini_options] with testpaths=["server/tests"] and asyncio_mode="auto".

    2. Run `uv sync` to install all dependencies.

    3. Create the directory structure: server/app/, server/app/models/, server/app/routers/, server/app/services/, server/tests/. All directories get __init__.py files.

    4. Create server/app/models/enums.py with FileType(str, Enum) having FILE and DIRECTORY variants.

    5. Create server/app/models/schemas.py with Pydantic BaseModel classes:
       - FileEntry: name (str), size (int), size_display (str), type (FileType), modified (str)
       - DirectoryListing: path (str), entries (list[FileEntry])

    6. Create server/app/exceptions.py with PathTraversalError(Exception) -- include the attempted path in the error message.

    7. Create server/app/config.py with ServerConfig class holding shared_folder (Path) and port (int). Include a module-level _config variable and get_server_config() / set_server_config() functions. ServerConfig.__init__ validates that shared_folder exists and is a directory, raising ValueError if not. Add a function create_config_from_args(args: argparse.Namespace) that constructs ServerConfig from parsed CLI args.

    8. Create server/app/services/file_service.py with:
       - resolve_safe_path(base_dir: Path, user_path: str) -> Path: resolves the path, validates it stays within base_dir using Path.resolve() and is_relative_to(), raises PathTraversalError if not, raises FileNotFoundError if target does not exist.
       - list_directory(base_dir: Path, relative_path: str) -> DirectoryListing: calls resolve_safe_path first, then iterates directory contents building FileEntry objects. Uses os.stat for size and mtime. Formats mtime as ISO 8601 string. Formats size_display using a format_file_size() helper (reuse logic from existing get_file_size in wifi_file_server.py but with strict types). Determines FileType from stat results.

    9. Create server/tests/conftest.py with fixtures:
       - tmp_shared_folder: creates a temp directory with sample files (test.txt with content, subdir/ with nested.txt, empty_dir/). Returns the Path.
       - Use pytest tmp_path fixture as base.

    10. Create server/tests/test_file_service.py: tests for resolve_safe_path (all behavior cases above) and list_directory (listing root, subdirectory, traversal attempt). Create a symlink test that creates a symlink pointing outside tmp_path and verifies PathTraversalError is raised.

    11. Create server/tests/test_config.py: tests for ServerConfig validation (valid folder, nonexistent folder raises ValueError, file instead of directory raises ValueError).

    12. Run `uv run pytest server/tests/test_file_service.py server/tests/test_config.py -x` -- tests must pass.
  </action>
  <verify>
    <automated>cd /Users/rahul/Projects/wifi-ftp-server && uv run pytest server/tests/test_file_service.py server/tests/test_config.py -x -v</automated>
  </verify>
  <done>
    - resolve_safe_path blocks ../, absolute paths, encoded traversals, and symlinks escaping base
    - list_directory returns DirectoryListing with correct FileEntry objects including name, size, size_display, type (enum), modified
    - ServerConfig validates folder exists and is a directory
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FastAPI app with files router, CORS, and SPA catch-all</name>
  <files>
    server/app/main.py,
    server/app/routers/__init__.py,
    server/app/routers/files.py,
    server/tests/test_routes_files.py,
    server/tests/test_cors.py
  </files>
  <behavior>
    - GET /api/files returns 200 with JSON body matching DirectoryListing schema (path, entries array)
    - GET /api/files?path=subdir returns listing of subdirectory
    - GET /api/files?path=../etc returns 403 with error message
    - GET /api/files?path=nonexistent returns 404
    - All API responses include Access-Control-Allow-Origin header
    - OPTIONS /api/files returns CORS preflight response with correct headers
  </behavior>
  <action>
    1. Create server/app/routers/files.py with an APIRouter(prefix="/api", tags=["files"]). Add GET /files endpoint accepting query param path: str = "" (empty string means root). The handler calls get_server_config() to get the shared folder, then calls list_directory(). Add exception handlers: PathTraversalError -> 403 JSON response, FileNotFoundError -> 404 JSON response.

    2. Create server/app/main.py with create_app() factory function:
       - Creates FastAPI(title="WiFi File Server")
       - Adds CORSMiddleware with allow_origins=["*"], allow_methods=["*"], allow_headers=["*"]. Do NOT set allow_credentials=True (mutually exclusive with wildcard origins per CORS spec).
       - Includes the files router via app.include_router(files.router)
       - Adds SPA catch-all: if client/dist exists, mount /assets as StaticFiles, then add catch-all route /{path:path} that serves index.html or matching static file. If client/dist does not exist, skip SPA mounting (development mode where Vite serves frontend).
       - Module-level: app = create_app()

    3. Create server/tests/test_routes_files.py using httpx AsyncClient with ASGITransport against the app. Tests:
       - GET /api/files returns 200 with valid DirectoryListing JSON
       - Response entries contain expected test files from tmp_shared_folder fixture
       - GET /api/files?path=subdir returns 200 with subdirectory contents
       - GET /api/files?path=../etc returns 403
       - GET /api/files?path=nonexistent returns 404
       Update conftest.py to add a fixture that creates the app with a temp shared folder configured via set_server_config().

    4. Create server/tests/test_cors.py: test that GET /api/files response includes Access-Control-Allow-Origin header. Test that OPTIONS /api/files returns 200 with CORS headers.

    5. Run full test suite: `uv run pytest server/tests/ -x`
  </action>
  <verify>
    <automated>cd /Users/rahul/Projects/wifi-ftp-server && uv run pytest server/tests/ -x -v</automated>
  </verify>
  <done>
    - GET /api/files returns correct JSON with file entries
    - Path traversal attempts return 403
    - CORS headers present on all API responses
    - All tests pass including file_service, config, routes, and CORS tests
  </done>
</task>

<task type="auto">
  <name>Task 3: Create CLI entry point with server startup</name>
  <files>
    server/app/cli.py,
    server/tests/test_cli.py
  </files>
  <action>
    1. Create server/app/cli.py with a main() function:
       - Uses argparse to parse: folder (positional, required), --port/-p (int, type=int), --host (str). Do NOT use default parameters -- instead create a separate function run_with_defaults(folder: str) that calls main() logic with port=8000 and host="0.0.0.0" (comment: "Default port 8000 for FastAPI convention, 0.0.0.0 for LAN access").
       - Validates the folder path using ServerConfig (which raises ValueError for bad paths).
       - Calls set_server_config() with the created ServerConfig.
       - Prints startup info: shared folder path, server URL with detected IP, local URL.
       - Starts uvicorn programmatically: uvicorn.run("server.app.main:app", host=host, port=port). Note: use the import string form so uvicorn can reload.
       - Wrap the uvicorn.run in try/except for KeyboardInterrupt (clean exit message) and Exception (error message + sys.exit(1)).
       - Add if __name__ == "__main__": main() guard.

    2. Update pyproject.toml to add [project.scripts] section: wifi-file-server = "server.app.cli:main"

    3. Create server/tests/test_cli.py: test that argparse parses valid args correctly (folder + port + host). Test that missing folder argument causes SystemExit. Test that the run_with_defaults function signature exists with exactly one parameter (folder: str). Do NOT test actual server startup (that requires binding a port).

    4. Run full test suite.
  </action>
  <verify>
    <automated>cd /Users/rahul/Projects/wifi-ftp-server && uv run pytest server/tests/ -x -v</automated>
  </verify>
  <done>
    - CLI entry point parses folder, --port, --host arguments
    - pyproject.toml has script entry point configured
    - Server can be started with `uv run wifi-file-server /some/folder --port 8000`
    - All tests pass
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `uv run pytest server/tests/ -x -v` -- all tests pass
2. Manual smoke test: `uv run wifi-file-server /tmp --port 8000` starts the server, `curl http://localhost:8000/api/files` returns JSON with file listing
</verification>

<success_criteria>
- FastAPI server starts from CLI with folder path argument
- GET /api/files returns JSON directory listing with file metadata
- Path traversal attacks (../, absolute paths, symlinks) are blocked with 403
- CORS headers present on all API responses
- All backend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-discovery/01-01-SUMMARY.md`
</output>
