---
phase: 09-relay-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - relay/__init__.py
  - relay/app/__init__.py
  - relay/app/main.py
  - relay/app/enums.py
  - relay/app/exceptions.py
  - relay/app/services/__init__.py
  - relay/app/services/mount_registry.py
  - relay/app/routers/__init__.py
  - relay/app/routers/landing.py
  - relay/templates/base.html
  - relay/templates/landing.html
  - relay/templates/not_found.html
  - relay/templates/offline.html
  - relay/templates/expired.html
  - tests/relay/conftest.py
  - tests/relay/test_mount_registry.py
  - tests/relay/test_landing.py
  - pyproject.toml
autonomous: true
requirements:
  - RELY-01
  - RELY-03
  - RELY-04

must_haves:
  truths:
    - "MountRegistry register/deregister/get_connection lifecycle works correctly"
    - "get_connection raises MountNotFoundError for unknown codes, MountOfflineError for offline mounts, MountExpiredError for expired mounts"
    - "Landing page renders at GET / with informational text and code input form"
    - "GET /?code=XXXX redirects 302 to /m/XXXX/"
    - "Error templates render without errors and extend base.html"
    - "not_found template includes a code input field for retry"
  artifacts:
    - path: "relay/app/services/mount_registry.py"
      provides: "MountRegistry with register, deregister, get_connection, generate_mount_code"
      exports: ["MountRegistry", "get_registry", "set_registry", "generate_mount_code"]
    - path: "relay/app/enums.py"
      provides: "MountStatus enum"
      exports: ["MountStatus"]
    - path: "relay/app/exceptions.py"
      provides: "Mount lifecycle exception hierarchy"
      exports: ["MountNotFoundError", "MountOfflineError", "MountExpiredError"]
    - path: "relay/app/routers/landing.py"
      provides: "Landing page router"
      exports: ["router"]
    - path: "relay/app/main.py"
      provides: "Relay app factory"
      exports: ["create_relay_app"]
    - path: "relay/templates/base.html"
      provides: "Shared Jinja2 layout"
    - path: "relay/templates/landing.html"
      provides: "Landing page template"
    - path: "relay/templates/not_found.html"
      provides: "Not found error page with code input"
    - path: "relay/templates/offline.html"
      provides: "Offline error page"
    - path: "relay/templates/expired.html"
      provides: "Expired error page"
  key_links:
    - from: "relay/app/routers/landing.py"
      to: "relay/templates/landing.html"
      via: "Jinja2Templates.TemplateResponse"
      pattern: "TemplateResponse.*landing\\.html"
    - from: "relay/app/services/mount_registry.py"
      to: "relay/app/exceptions.py"
      via: "raises typed exceptions"
      pattern: "raise Mount(NotFound|Offline|Expired)Error"
    - from: "relay/app/main.py"
      to: "relay/app/routers/landing.py"
      via: "app.include_router"
      pattern: "include_router.*landing"
---

<objective>
Create the relay server foundation: package structure, MountRegistry service, landing page, error templates, and app factory.

Purpose: Establishes the relay as a standalone FastAPI application with the mount registry (RELY-01), landing page (RELY-03), and error templates (RELY-04). Plan 02 builds the proxy and agent WebSocket on top of this foundation.
Output: Working relay app with landing page, registry service, and all Jinja2 templates.
</objective>

<execution_context>
@/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-relay-server/09-CONTEXT.md
@.planning/phases/09-relay-server/09-RESEARCH.md
@.planning/phases/09-relay-server/09-VALIDATION.md

<interfaces>
<!-- TunnelConnection API from Phase 8 — used by MountRegistry -->

From tunnel/connection.py:
```python
class TunnelConnection:
    def __init__(self, ws: WebSocketProtocol) -> None: ...
    def open_stream(self, request_id: uuid.UUID) -> StreamState: ...
    def close_stream(self, request_id: uuid.UUID) -> None: ...
    async def send_data(self, request_id: uuid.UUID, payload: bytes) -> None: ...
    async def send_open(self, request_id: uuid.UUID, metadata: dict) -> None: ...
    async def send_close(self, request_id: uuid.UUID) -> None: ...
    async def send_cancel(self, request_id: uuid.UUID) -> None: ...
    async def send_control(self, message: dict) -> None: ...
    async def receive_control(self) -> dict: ...
    async def run_receive_loop(self) -> None: ...
    def start_heartbeat(self, heartbeat_interval_s: float, missed_limit: int) -> None: ...
    async def read_stream(self, request_id: uuid.UUID, timeout_s: float) -> bytes: ...
    async def read_stream_iter(self, request_id: uuid.UUID) -> AsyncGenerator[bytes, None]: ...
    async def close(self) -> None: ...
```

From tunnel/constants.py:
```python
HEARTBEAT_INTERVAL_S: int = 15
HEARTBEAT_MISSED_LIMIT: int = 3
FIRST_BYTE_TIMEOUT_S: int = 30
```

From tunnel/enums.py:
```python
class FrameType(int, Enum):
    OPEN = 0x01; DATA = 0x02; CLOSE = 0x03; CANCEL = 0x04
    ERROR = 0x05; PING = 0x06; PONG = 0x07
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relay package foundation — enums, exceptions, registry service, app factory</name>
  <files>
    relay/__init__.py,
    relay/app/__init__.py,
    relay/app/enums.py,
    relay/app/exceptions.py,
    relay/app/services/__init__.py,
    relay/app/services/mount_registry.py,
    relay/app/routers/__init__.py,
    relay/app/main.py,
    tests/relay/conftest.py,
    tests/relay/test_mount_registry.py,
    pyproject.toml
  </files>
  <behavior>
    - register(code, connection) stores mount; get_connection(code) returns the same connection
    - register with empty code raises ValueError
    - deregister(code) removes mount; subsequent get_connection raises MountNotFoundError
    - deregister unknown code raises MountNotFoundError
    - get_connection on OFFLINE mount raises MountOfflineError
    - get_connection on EXPIRED mount raises MountExpiredError
    - generate_mount_code() returns 8-character URL-safe string
    - set_registry / get_registry allows test injection of fresh registry instances
    - MountStatus enum has ONLINE, OFFLINE, EXPIRED values (str, Enum)
  </behavior>
  <action>
    Create the relay package tree:

    1. `relay/__init__.py` and `relay/app/__init__.py` — empty package markers.
    2. `relay/app/enums.py` — `MountStatus(str, Enum)` with ONLINE, OFFLINE, EXPIRED. Follow `server/app/models/enums.py` pattern exactly.
    3. `relay/app/exceptions.py` — `MountNotFoundError(code: str)`, `MountOfflineError(code: str)`, `MountExpiredError(code: str)`. Each stores `self.code`. Follow `server/app/exceptions.py` pattern.
    4. `relay/app/services/__init__.py` — empty.
    5. `relay/app/services/mount_registry.py`:
       - `MountRecord` dataclass with fields: `code: str`, `connection: TunnelConnection`, `status: MountStatus`
       - `MountRegistry` class with `_mounts: dict[str, MountRecord]`
       - Methods: `register(code: str, connection: TunnelConnection) -> None`, `deregister(code: str) -> None`, `get_connection(code: str) -> TunnelConnection`, `mark_offline(code: str) -> None`, `has_mount(code: str) -> bool`
       - `register` validates code is non-empty (raise ValueError).
       - `get_connection` raises MountNotFoundError / MountOfflineError / MountExpiredError based on status.
       - `deregister` raises MountNotFoundError if code not present.
       - Module-level `_registry: MountRegistry | None` with `get_registry() -> MountRegistry` and `set_registry(r: MountRegistry) -> None`. `get_registry` raises RuntimeError if not initialized.
       - Standalone `generate_mount_code() -> str` using `secrets.token_urlsafe(6)` (returns 8 chars).
    6. `relay/app/routers/__init__.py` — empty.
    7. `relay/app/main.py` — `create_relay_app() -> FastAPI`:
       - Creates FastAPI(title="Network File Server Relay")
       - Adds CORSMiddleware with allow_origins=["*"], allow_methods=["*"], allow_headers=["*"]
       - Includes landing router (agent_ws and mount_proxy routers added in Plan 02)
       - Initializes a MountRegistry via set_registry(MountRegistry()) in the factory
       - `app = create_relay_app()` at module level for uvicorn
    8. `tests/relay/conftest.py`:
       - Do NOT create `tests/relay/__init__.py` (same reason as tests/tunnel/ — prevents sys.path shadowing of the real relay/ package).
       - `relay_app` fixture that calls `create_relay_app()` and sets a fresh MountRegistry via `set_registry`.
       - `relay_client` fixture using `httpx.AsyncClient` with `ASGITransport(app=relay_app)`.
       - `MockTunnelConnection` class that mocks the TunnelConnection interface for registry tests — needs attributes accessed by registry (just needs to be a non-None object for now; Plan 02 will expand it).
    9. `tests/relay/test_mount_registry.py` — Tests for all registry behaviors listed in `<behavior>`.
    10. `pyproject.toml` — Add "relay" to `[tool.hatch.build.targets.wheel] packages` list alongside "server" and "tunnel". Add "tests/relay" or ensure testpaths covers it.
  </action>
  <verify>
    <automated>uv run pytest tests/relay/test_mount_registry.py -x -v</automated>
  </verify>
  <done>
    MountRegistry register/deregister/get_connection works with typed exceptions. MountStatus enum enforces ONLINE/OFFLINE/EXPIRED. generate_mount_code returns 8-char strings. get_registry/set_registry pattern works for test injection. relay package importable.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Landing page, error templates, and landing router</name>
  <files>
    relay/templates/base.html,
    relay/templates/landing.html,
    relay/templates/not_found.html,
    relay/templates/offline.html,
    relay/templates/expired.html,
    relay/app/routers/landing.py,
    relay/app/main.py,
    tests/relay/test_landing.py
  </files>
  <behavior>
    - GET / returns 200 with HTML containing a form input for mount code and informational text about Network File Server
    - GET /?code=testcode returns 302 redirect to /m/testcode/
    - GET /?code= (empty string) returns 200 with landing page (no redirect)
    - not_found.html renders with code input field for retry
    - offline.html renders with "offline" message
    - expired.html renders with "expired" message
    - All templates extend base.html (shared layout with .card container)
  </behavior>
  <action>
    Create Jinja2 templates and the landing router:

    1. `relay/templates/base.html` — Shared layout per RESEARCH.md Pattern 5. System font stack, centered card, light/dark mode via prefers-color-scheme. Contains `{% block title %}` and `{% block content %}` blocks.

    2. `relay/templates/landing.html` — Extends base.html. Title: "Network File Server". Content:
       - Heading: "Network File Server"
       - Brief paragraph explaining what mount codes are and how to use them (informational per CONTEXT.md)
       - Form with `method="get"` and `action="/"`: text input `name="code"` with placeholder "Enter mount code", submit button "Connect"

    3. `relay/templates/not_found.html` — Extends base.html. Title: "Mount Not Found". Content:
       - Heading: "Mount Not Found"
       - Message: "No mount exists with this code. It may have been disconnected."
       - Same code input form as landing page so user can try a different code (per CONTEXT.md locked decision)

    4. `relay/templates/offline.html` — Extends base.html. Title: "Mount Offline". Content:
       - Heading: "Mount Offline"
       - Message: "This mount is currently offline. The owner may reconnect soon." (exact wording from CONTEXT.md)

    5. `relay/templates/expired.html` — Extends base.html. Title: "Mount Expired". Content:
       - Heading: "Mount Expired"
       - Message: "This mount has expired and is no longer accessible."

    6. `relay/app/routers/landing.py`:
       - `router = APIRouter()`
       - Template directory resolved via `Path(__file__).resolve().parent.parent.parent / "templates"` (relay/app/routers -> relay/app -> relay -> relay/templates)
       - `templates = Jinja2Templates(directory=str(template_dir))`
       - `GET /` handler: if `code` query param is non-empty string, return `RedirectResponse(url=f"/m/{code}/", status_code=302)`. Otherwise return `TemplateResponse(request, "landing.html")`.
       - Use `code: str = Query("")` — NO default parameter per project rules, but Query("") is a FastAPI dependency, not a function default. Actually, to be strict: define as `code: str = Query(...)` then check, or use the simpler approach: define the param with a required empty-string default via `Query("")`. The key is the handler signature must not use Python default params.
       - Export `templates` so mount_proxy.py (Plan 02) can reuse the same Jinja2Templates instance for error pages.

    7. Update `relay/app/main.py` to include the landing router.

    8. `tests/relay/test_landing.py`:
       - Test GET / returns 200 with "Network File Server" in body and a form input
       - Test GET /?code=abc123 returns 302 with Location header /m/abc123/
       - Test GET /?code= (empty) returns 200 (landing page, no redirect)
       - Test that not_found, offline, expired templates render by importing templates and calling `get_template("not_found.html").render()` etc. (template smoke tests)
  </action>
  <verify>
    <automated>uv run pytest tests/relay/test_landing.py -x -v</automated>
  </verify>
  <done>
    Landing page renders at GET / with informational text and code input. Code submission redirects to /m/{code}/. All four Jinja2 templates (landing, not_found, offline, expired) render without errors. not_found includes code input for retry.
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/relay/ -x -v
</verification>

<success_criteria>
- relay/ package is importable and included in pyproject.toml build targets
- MountRegistry correctly manages mount lifecycle with typed exceptions
- GET / renders landing page with code input form
- GET /?code=XXX redirects 302 to /m/XXX/
- All 5 Jinja2 templates render correctly (base, landing, not_found, offline, expired)
- not_found.html includes code input for retry
- All tests pass in tests/relay/
</success_criteria>

<output>
After completion, create `.planning/phases/09-relay-server/09-01-SUMMARY.md`
</output>
