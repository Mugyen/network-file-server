---
phase: 11-remote-access-and-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/utils/remoteMount.ts
  - client/src/api/client.ts
  - client/src/main.tsx
  - client/src/hooks/useWebSocket.ts
  - client/src/components/ModeBadges.tsx
  - client/src/App.tsx
  - client/src/api/auth.ts
  - client/vitest.config.ts
  - client/src/utils/remoteMount.test.ts
autonomous: true
requirements:
  - RMUI-01

must_haves:
  truths:
    - "SPA detects /m/{code} prefix from window.location.pathname in remote mode"
    - "API calls use /m/{code}/api prefix when running through relay"
    - "API calls use /api prefix in LAN mode (no regression)"
    - "WebSocket URL uses /m/{code}/ws in remote mode"
    - "Remote badge pill shows in header when running through relay"
    - "Auth probe in main.tsx uses dynamic API base"
  artifacts:
    - path: "client/src/utils/remoteMount.ts"
      provides: "Runtime mount detection and URL helpers"
      exports: ["getApiBase", "getWsUrl", "isRemoteMount", "getMountPrefix"]
    - path: "client/vitest.config.ts"
      provides: "Vitest configuration for client unit tests"
    - path: "client/src/utils/remoteMount.test.ts"
      provides: "Unit tests for remote mount detection"
  key_links:
    - from: "client/src/api/client.ts"
      to: "client/src/utils/remoteMount.ts"
      via: "import getApiBase"
      pattern: "getApiBase"
    - from: "client/src/hooks/useWebSocket.ts"
      to: "client/src/utils/remoteMount.ts"
      via: "import getWsUrl"
      pattern: "getWsUrl"
    - from: "client/src/App.tsx"
      to: "client/src/utils/remoteMount.ts"
      via: "import isRemoteMount"
      pattern: "isRemoteMount"
    - from: "client/src/main.tsx"
      to: "client/src/utils/remoteMount.ts"
      via: "import getApiBase for auth probe"
      pattern: "getApiBase"
---

<objective>
Adapt the React SPA to work seamlessly through the relay proxy by detecting the mount context from the URL and dynamically prefixing all API and WebSocket calls.

Purpose: When the SPA is served at `/m/{code}/`, all API calls must target `/m/{code}/api/*` and WebSocket must connect to `/m/{code}/ws`. A "Remote" badge indicates remote mode. In LAN mode, behavior is unchanged.

Output: remoteMount.ts utility module, updated client.ts/main.tsx/useWebSocket.ts, Remote badge in ModeBadges, vitest setup.
</objective>

<execution_context>
@/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-remote-access-and-hardening/11-CONTEXT.md
@.planning/phases/11-remote-access-and-hardening/11-RESEARCH.md

@client/src/api/client.ts
@client/src/hooks/useWebSocket.ts
@client/src/main.tsx
@client/src/App.tsx
@client/src/components/ModeBadges.tsx
@client/src/api/auth.ts
@client/src/api/serverInfo.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From client/src/api/client.ts:
```typescript
const API_BASE = "/api";  // Must become dynamic via getApiBase()
export function uploadWithProgress(...): Promise<UploadResult[]>;
// XHR URL uses `${API_BASE}/files/upload?path=...` — must use getApiBase()
```

From client/src/hooks/useWebSocket.ts:
```typescript
// Line 43: const url = `${protocol}//${window.location.host}/ws?device_name=...`
// Must use getWsUrl("/ws", `device_name=${encodeURIComponent(deviceName)}`)
```

From client/src/main.tsx:
```typescript
// Line 32: const probe = await fetch("/api/files", { credentials: "include" });
// Must use `${getApiBase()}/files`
```

From client/src/components/ModeBadges.tsx:
```typescript
interface ModeBadgesProps {
  readOnly: boolean;
  passwordProtected: boolean;
  // Need to add: remote: boolean
}
```

From client/src/api/auth.ts (needs update):
```typescript
// Uses API_BASE for login/logout — must import from client.ts or use getApiBase()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: remoteMount.ts utility with vitest setup and tests</name>
  <files>
    client/src/utils/remoteMount.ts,
    client/vitest.config.ts,
    client/src/utils/remoteMount.test.ts
  </files>
  <behavior>
    - detectMountPrefix returns "" for pathname "/"
    - detectMountPrefix returns "" for pathname "/some/path"
    - detectMountPrefix returns "/m/ABC12345" for pathname "/m/ABC12345/"
    - detectMountPrefix returns "/m/ABC12345" for pathname "/m/ABC12345/api/files"
    - getApiBase returns "/api" when MOUNT_PREFIX is ""
    - getApiBase returns "/m/ABC12345/api" when MOUNT_PREFIX is "/m/ABC12345"
    - isRemoteMount returns false when MOUNT_PREFIX is ""
    - isRemoteMount returns true when MOUNT_PREFIX is "/m/ABC12345"
    - getWsUrl("/ws", "device_name=test") returns correct ws:// URL with mount prefix in remote mode
    - getWsUrl("/ws", "device_name=test") returns correct ws:// URL without prefix in LAN mode
  </behavior>
  <action>
    1. Set up vitest for the client project:
       - Check if vitest is in client/package.json devDependencies; if not, install: `cd client && npm install -D vitest jsdom`
       - Create `client/vitest.config.ts` with jsdom environment for browser API mocking

    2. Create `client/src/utils/remoteMount.ts`:
       - `detectMountPrefix(): string` — internal function that reads `window.location.pathname` and extracts `/m/{code}` via regex `/^(\/m\/[^/]+)/`
       - `MOUNT_PREFIX: string` — module-level constant computed once via `detectMountPrefix()`
       - `getApiBase(): string` — returns `MOUNT_PREFIX === "" ? "/api" : \`${MOUNT_PREFIX}/api\``
       - `isRemoteMount(): boolean` — returns `MOUNT_PREFIX !== ""`
       - `getMountPrefix(): string` — returns `MOUNT_PREFIX`
       - `getWsUrl(wsPath: string, queryString: string): string` — builds full ws:// URL with protocol detection, host, and mount prefix

    3. Write tests in `client/src/utils/remoteMount.test.ts`:
       - Since MOUNT_PREFIX is computed at module load, tests must mock window.location.pathname BEFORE importing
       - Use vitest's `vi.stubGlobal` or dynamic import to test both LAN and remote modes
       - Test all exported functions for both modes
  </action>
  <verify>
    <automated>cd /Users/rahul/Projects/network-file-server/client && npx vitest run src/utils/remoteMount.test.ts</automated>
  </verify>
  <done>
    remoteMount.ts exports getApiBase, getWsUrl, isRemoteMount, getMountPrefix.
    Vitest configured and tests pass for both LAN and remote mode URL detection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire remoteMount into all API call sites and add Remote badge</name>
  <files>
    client/src/api/client.ts,
    client/src/main.tsx,
    client/src/hooks/useWebSocket.ts,
    client/src/components/ModeBadges.tsx,
    client/src/App.tsx,
    client/src/api/auth.ts
  </files>
  <action>
    1. Update `client/src/api/client.ts`:
       - Import `getApiBase` from `../utils/remoteMount.ts`
       - Change `const API_BASE = "/api"` to `const API_BASE = getApiBase()`
       - In `uploadWithProgress()`: the XHR URL already uses `${API_BASE}/files/upload?...` which is correct since API_BASE is now dynamic. Verify no other hardcoded `/api/` strings exist.

    2. Update `client/src/main.tsx`:
       - Import `getApiBase` from `./utils/remoteMount.ts`
       - Change the auth probe from `fetch("/api/files", ...)` to `fetch(\`${getApiBase()}/files\`, ...)`

    3. Update `client/src/hooks/useWebSocket.ts`:
       - Import `getWsUrl` from `../utils/remoteMount.ts`
       - Replace the hardcoded URL construction:
         ```
         const url = `${protocol}//${window.location.host}/ws?device_name=${encodeURIComponent(deviceName)}`;
         ```
         with:
         ```
         const url = getWsUrl("/ws", `device_name=${encodeURIComponent(deviceName)}`);
         ```

    4. Update `client/src/api/auth.ts`:
       - Check if it hardcodes `/api/auth/login` and `/api/auth/logout` or uses the client.ts helpers
       - If it hardcodes paths, import `getApiBase` and prefix accordingly
       - The login/logout must go through `/m/{code}/api/auth/login` in remote mode

    5. Update `client/src/components/ModeBadges.tsx`:
       - Add `remote: boolean` to ModeBadgesProps interface
       - Add a "Remote" pill badge (Globe icon from lucide-react) with green styling:
         ```
         bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-300
         ```
       - Show when `remote` is true
       - Import `Globe` from lucide-react (matching Lock icon pattern)

    6. Update `client/src/App.tsx`:
       - Import `isRemoteMount` from `./utils/remoteMount.ts`
       - Pass `remote={isRemoteMount()}` to `<ModeBadges>` component
  </action>
  <verify>
    <automated>cd /Users/rahul/Projects/network-file-server/client && npx tsc --noEmit && npx vite build</automated>
  </verify>
  <done>
    API_BASE is dynamically computed from window.location.pathname.
    WebSocket URL includes /m/{code} prefix in remote mode.
    Auth probe in main.tsx uses dynamic API base.
    "Remote" pill badge appears in header when isRemoteMount() is true.
    TypeScript compiles clean. Vite build succeeds.
    LAN mode behavior unchanged (no /m/ prefix in URL = same as before).
  </done>
</task>

</tasks>

<verification>
cd /Users/rahul/Projects/network-file-server/client && npx vitest run && npx tsc --noEmit && npx vite build
</verification>

<success_criteria>
- remoteMount.ts correctly detects LAN vs remote mode from URL
- All 4 API call sites (client.ts API_BASE, client.ts uploadWithProgress, main.tsx probe, useWebSocket.ts) use dynamic URLs
- "Remote" badge appears in header when SPA is loaded from /m/{code}/ path
- TypeScript compiles without errors, Vite build succeeds
- No regression in LAN mode behavior
</success_criteria>

<output>
After completion, create `.planning/phases/11-remote-access-and-hardening/11-02-SUMMARY.md`
</output>
