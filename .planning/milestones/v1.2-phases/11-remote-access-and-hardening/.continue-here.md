---
phase: 11-remote-access-and-hardening
task: UAT
total_tasks: 8
status: in_progress
last_updated: 2026-03-12T12:30:00Z
---

<current_state>
Phase 11 UAT in progress. Three connection stability bugs fixed this session:

1. **Heartbeat ping→pong** (985e88c): Neither side responded to pings with pongs — both sent pings but neither replied. Fixed by adding ping responders, making relay the sole heartbeat initiator.
2. **Receive loop disconnect crash** (0c5ccd3): Starlette's `{"type": "websocket.disconnect"}` message had no handler — loop silently continued, hitting RuntimeError on next receive(). Fixed with `else: break`.
3. **Stream teardown race** (0c5ccd3): `_dispatch_frame` crashed on `StreamNotFoundError` during WS close race. Made tolerant. Also fixed `handle_ws_open_frame` not calling `open_stream(ws_id)`.

**Test 2 re-verified via Chrome**: Login works, session cookie persists after page reload (cookie path `/m/{code}/` is correct). File browser loads with Protected+Remote badges.

**Remaining issue**: The SPA's `form_input` via browser automation doesn't trigger React state updates — `MOUNT_PREFIX` is computed at JS module load time and cached. If the JS bundle is cached from a prior mount code, API calls go to the wrong URL. Hard refresh (Cmd+Shift+R) fixes this. This is a browser caching issue, not a bug in the app.

**WebSocket "Reconnecting..." banner**: The SPA shows "Reconnecting..." because the device discovery WebSocket at `/m/{code}/ws` may need investigation — the relay rejected WS connections with 403 in logs. This needs checking but doesn't block HTTP-based UAT.
</current_state>

<completed_work>

- Bug fix: Heartbeat ping→pong responders (985e88c)
- Bug fix: Receive loop disconnect + stream race (0c5ccd3)
- Test 1: Cold Start Smoke Test — PASS
- Test 2: Mount with Password Protection — PASS (re-verified after fixes)
- Test 6: Remote Badge Display — PASS
</completed_work>

<remaining_work>

- Test 3: Password Cookie Isolation — pending
- Test 4: TTL Auto-Expiry — pending
- Test 5: SPA File Browsing Through Relay — pending
- Test 7: Real-Time Clipboard Sync Through Relay — pending
- Test 8: Real-Time Transfer Notifications Through Relay — pending
- Investigate: WS 403 rejections for device discovery through relay
</remaining_work>

<decisions_made>

- Relay is the sole heartbeat initiator; agent only responds to pings
- `_dispatch_frame` silently ignores frames for already-removed streams (teardown race is normal)
- `run_receive_loop` breaks on disconnect messages (no "bytes"/"text" key)
- `handle_ws_open_frame` must call `conn.open_stream(ws_id)` before reading WS_DATA
</decisions_made>

<blockers>
None — connection is now stable. WS 403 issue may affect Tests 7-8 (real-time features) but not HTTP tests.
</blockers>

<context>
Relay startup: `uv run uvicorn relay.app.main:app --port 8001`
Agent startup: `uv run wifi-file-server mount ./test-files --server http://localhost:8001`
Use `PYTHONUNBUFFERED=1` to see agent output immediately.
Test files dir: `./test-files/` (created with readme.txt, notes.txt, page.html, photos/)
</context>

<next_action>
1. Update 11-UAT.md to mark Test 2 as pass
2. Resume UAT from Test 3 with `/gsd:verify-work 11`
3. Investigate WS 403 rejections before Tests 7-8
</next_action>
