---
phase: 11-remote-access-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/duration.py
  - agent/exceptions.py
  - agent/connection.py
  - agent/cli.py
  - server/app/config.py
  - server/app/routers/auth.py
  - server/app/cli.py
  - tests/agent/test_duration.py
  - tests/agent/test_cli.py
  - tests/agent/test_agent_connection.py
  - server/tests/test_auth_cookie_path.py
  - server/tests/test_config_mount_code.py
autonomous: true
requirements:
  - ACCS-01
  - ACCS-02

must_haves:
  truths:
    - "Mount owner can set --password on mount subcommand and remote users must authenticate"
    - "Cookie path is scoped to /m/{code}/ so different mounts get isolated sessions"
    - "Mount auto-expires after --ttl duration and agent exits cleanly"
    - "Agent does NOT reconnect after TTL expiry"
    - "Terminal shows remaining TTL countdown"
  artifacts:
    - path: "agent/duration.py"
      provides: "parse_duration function for human-readable TTL strings"
      exports: ["parse_duration"]
    - path: "agent/exceptions.py"
      provides: "AgentExpiredError for TTL-triggered exit"
      exports: ["AgentExpiredError"]
    - path: "server/app/config.py"
      provides: "ServerConfig with mount_code field"
      contains: "mount_code"
    - path: "server/app/routers/auth.py"
      provides: "Cookie path scoped to /m/{code}/ for remote mounts"
      contains: "mount_code"
  key_links:
    - from: "server/app/cli.py"
      to: "agent/duration.py"
      via: "--ttl type=parse_duration"
      pattern: "parse_duration"
    - from: "agent/connection.py"
      to: "agent/exceptions.py"
      via: "raise AgentExpiredError on TTL expiry"
      pattern: "AgentExpiredError"
    - from: "agent/connection.py"
      to: "server/app/config.py"
      via: "ServerConfig mount_code passed through"
      pattern: "mount_code=assigned_code"
    - from: "server/app/routers/auth.py"
      to: "server/app/config.py"
      via: "get_server_config().mount_code for cookie path"
      pattern: "mount_code"
---

<objective>
Add password protection and TTL auto-expiry to the mount subcommand.

Purpose: Mount owners can secure remote mounts with passwords (reusing v1.1 auth) and set time limits so mounts automatically expire. Cookie path scoping prevents cross-mount session leakage on the same relay domain.

Output: Extended CLI flags, duration parser, TTL timer, cookie path scoping, AgentExpiredError exception.
</objective>

<execution_context>
@/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-remote-access-and-hardening/11-CONTEXT.md
@.planning/phases/11-remote-access-and-hardening/11-RESEARCH.md

@server/app/config.py
@server/app/routers/auth.py
@server/app/cli.py
@agent/connection.py
@agent/cli.py
@agent/display.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From server/app/config.py:
```python
class ServerConfig:
    shared_folder: Path
    port: int
    password_hash: bytes | None
    read_only: bool
    receive: bool

    def __init__(self, shared_folder, port, password_hash, read_only, receive): ...

def create_default_config(shared_folder: Path, port: int) -> ServerConfig: ...
def get_server_config() -> ServerConfig: ...
def set_server_config(config: ServerConfig) -> None: ...
def create_config_from_args(args: argparse.Namespace) -> ServerConfig: ...
```

From server/app/routers/auth.py:
```python
@router.post("/login")
def login(request: LoginRequest) -> JSONResponse:
    # Currently: response.set_cookie(key="session", value=token, httponly=True, samesite="lax", path="/")
```

From agent/connection.py:
```python
async def connect_and_serve(relay_url: str, folder: Path, name: str, preferred_code: str | None) -> str: ...
async def run_agent_loop(relay_url: str, folder: Path, name: str) -> None: ...
```

From server/app/cli.py:
```python
def _build_mount_parser() -> argparse.ArgumentParser: ...
def _parse_args(argv: Optional[list] = None) -> argparse.Namespace: ...
def run_mount(args: argparse.Namespace) -> None:  # in agent/cli.py
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Duration parser, AgentExpiredError, ServerConfig.mount_code, cookie path scoping</name>
  <files>
    agent/duration.py,
    agent/exceptions.py,
    server/app/config.py,
    server/app/routers/auth.py,
    tests/agent/test_duration.py,
    server/tests/test_auth_cookie_path.py,
    server/tests/test_config_mount_code.py
  </files>
  <behavior>
    - parse_duration("30m") returns 1800
    - parse_duration("2h") returns 7200
    - parse_duration("1d") returns 86400
    - parse_duration("90s") returns 90
    - parse_duration("abc") raises ValueError
    - parse_duration("30x") raises ValueError
    - parse_duration("") raises ValueError
    - ServerConfig with mount_code=None works (LAN mode backward compat)
    - ServerConfig with mount_code="ABC12345" stores it correctly
    - create_default_config still works without mount_code
    - create_config_from_args still works without mount_code
    - auth login with mount_code=None sets cookie path="/"
    - auth login with mount_code="ABC12345" sets cookie path="/m/ABC12345/"
    - auth logout with mount_code="ABC12345" sets cookie path="/m/ABC12345/"
    - AgentExpiredError is a subclass of Exception
  </behavior>
  <action>
    1. Create `agent/duration.py` with `parse_duration(value: str) -> int` function:
       - Uses `re.compile(r"^(\d+)([smhd])$")` pattern
       - `_UNITS: dict[str, int] = {"s": 1, "m": 60, "h": 3600, "d": 86400}`
       - Raises ValueError with descriptive message on no match
       - No default parameters — value is required

    2. Create `agent/exceptions.py` with `AgentExpiredError(Exception)`:
       - Simple exception class with a message attribute
       - Constructor takes `message: str` parameter

    3. Extend `server/app/config.py`:
       - Add `mount_code: str | None` field to ServerConfig
       - Add `mount_code` parameter to `__init__` (required, no default — callers must pass explicitly)
       - Update `create_default_config` to pass `mount_code=None`
       - Update `create_config_from_args` to pass `mount_code=None`
       - IMPORTANT: All existing callers of ServerConfig() must be updated to pass `mount_code=None` explicitly (agent/connection.py, server/app/cli.py, test files)

    4. Update `server/app/routers/auth.py`:
       - In `login()`: read `config.mount_code`; compute `cookie_path = f"/m/{config.mount_code}/" if config.mount_code is not None else "/"`; use in `set_cookie(path=cookie_path)`
       - In `logout()`: same cookie_path logic for the expiry cookie

    5. Write tests first (TDD):
       - `tests/agent/test_duration.py`: all parse_duration cases
       - `server/tests/test_config_mount_code.py`: ServerConfig with/without mount_code
       - `server/tests/test_auth_cookie_path.py`: cookie path scoping via TestClient with mount_code set vs None
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_duration.py server/tests/test_config_mount_code.py server/tests/test_auth_cookie_path.py -x -v</automated>
  </verify>
  <done>
    parse_duration converts s/m/h/d strings to seconds and rejects invalid input.
    ServerConfig carries mount_code field; all existing callers pass mount_code=None.
    Auth cookie path is "/m/{code}/" when mount_code is set, "/" otherwise.
    AgentExpiredError exists for TTL expiry signaling.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI flags, TTL timer, connect_and_serve extension, no-retry on expiry</name>
  <files>
    server/app/cli.py,
    agent/cli.py,
    agent/connection.py,
    tests/agent/test_cli.py,
    tests/agent/test_agent_connection.py
  </files>
  <behavior>
    - _build_mount_parser() includes --password and --ttl flags
    - _parse_args(["mount", ".", "--server", "http://x", "--password", "secret"]) has password="secret"
    - _parse_args(["mount", ".", "--server", "http://x", "--ttl", "30m"]) has ttl_seconds=1800
    - _parse_args(["mount", ".", "--server", "http://x", "--ttl", "bad"]) exits with error
    - connect_and_serve accepts password_hash and ttl_seconds parameters
    - connect_and_serve passes mount_code to ServerConfig after receiving assigned_code
    - When ttl_seconds is set, TTL timer fires and raises AgentExpiredError
    - run_agent_loop catches AgentExpiredError and exits (does NOT retry)
    - run_agent_loop still retries on normal exceptions (ConnectionError etc.)
    - TTL countdown display prints remaining time periodically
  </behavior>
  <action>
    1. Update `server/app/cli.py` `_build_mount_parser()`:
       - Add `--password` argument (type=str, help="Password to protect the remote mount (max 72 bytes)")
       - Add `--ttl` argument (type=parse_duration from agent.duration, dest="ttl_seconds", help="Auto-expire duration (e.g. 30m, 2h, 1d)")
       - Update `_parse_args()` to include password and ttl_seconds in the mount Namespace

    2. Update `agent/cli.py` `run_mount()`:
       - Import hash_password from server.app.services.auth_service
       - Import parse_duration (already used via argparse type= but needed for validation messaging)
       - Validate password length (max 72 bytes, same as LAN mode)
       - Hash password if provided: `password_hash = hash_password(args.password)` if args.password is not None else None
       - Pass password_hash and ttl_seconds to run_agent_loop

    3. Update `agent/connection.py`:
       - Extend `connect_and_serve` signature: add `password_hash: bytes | None` and `ttl_seconds: int | None` parameters
       - Pass `password_hash=password_hash` and `mount_code=assigned_code` to ServerConfig constructor
       - Set up AuthTokenService and set_token_service when password_hash is not None (import from auth_service)
       - After starting heartbeat, if ttl_seconds is not None: create TTL timer task `_ttl_countdown(ttl_seconds)` and TTL display task `_print_ttl_countdown(ttl_seconds)`
       - `_ttl_countdown` sleeps for ttl_seconds then calls `conn.close()`; sets a flag or raises AgentExpiredError
       - Use a `_ttl_expired` flag: after receive loop exits, check flag. If True, raise AgentExpiredError
       - Cancel TTL tasks in finally block if they exist

       - Extend `run_agent_loop` signature: add `password_hash: bytes | None` and `ttl_seconds: int | None` parameters
       - Pass them through to connect_and_serve
       - Catch AgentExpiredError: print "Mount expired" and break (no retry)

       - Add `_print_ttl_countdown(ttl_seconds: int) -> None` coroutine:
         - Uses time.monotonic() for start time
         - Every 60 seconds prints "Expires in Xh Ym" or "Expires in Ym"
         - Returns when remaining <= 0

    4. Write/extend tests:
       - In `tests/agent/test_cli.py`: test --password and --ttl flags in mount parser, test invalid --ttl
       - In `tests/agent/test_agent_connection.py`: test TTL expiry triggers AgentExpiredError, test run_agent_loop does not retry on AgentExpiredError, test password_hash passed to ServerConfig
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_cli.py tests/agent/test_agent_connection.py -x -v</automated>
  </verify>
  <done>
    --password and --ttl flags work on mount subcommand.
    connect_and_serve creates TTL timer that expires the mount after duration.
    run_agent_loop exits cleanly (no retry) on TTL expiry.
    Password is hashed and passed through to ServerConfig/create_app.
    TTL countdown displays remaining time in terminal.
    All existing tests still pass.
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/ server/tests/ -x -q
</verification>

<success_criteria>
- `--password secret` on mount subcommand results in password-protected remote mount (cookie path scoped to /m/{code}/)
- `--ttl 30m` on mount subcommand causes agent to auto-expire after 30 minutes
- Agent does not reconnect after TTL expiry (prints "Mount expired" and exits)
- All existing tests pass (no regressions from ServerConfig.mount_code addition)
</success_criteria>

<output>
After completion, create `.planning/phases/11-remote-access-and-hardening/11-01-SUMMARY.md`
</output>
