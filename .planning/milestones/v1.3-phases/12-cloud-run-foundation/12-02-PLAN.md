---
phase: 12-cloud-run-foundation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - relay/app/main.py
  - relay/app/middleware/__init__.py
  - relay/app/middleware/secure_cookies.py
  - tests/relay/test_secure_cookies.py
  - tests/relay/test_cors.py
  - tests/relay/test_proxy_headers.py
autonomous: true
requirements: [DEPLOY-04, DEPLOY-05, DEPLOY-06]

must_haves:
  truths:
    - "Session cookies carry the Secure flag when the relay is behind HTTPS (X-Forwarded-Proto: https)"
    - "CORS preflight from an unlisted origin is rejected when RELAY_ENV=production"
    - "CORS preflight from a listed origin returns Access-Control-Allow-Origin with that origin"
    - "CORS allows credentials (Access-Control-Allow-Credentials: true) in production mode"
    - "request.url.scheme reflects https when behind Cloud Run TLS termination"
    - "Dev mode CORS still allows wildcard origins for local development"
  artifacts:
    - path: "relay/app/middleware/secure_cookies.py"
      provides: "ASGI middleware that stamps Secure flag on Set-Cookie headers behind HTTPS"
      exports: ["SecureCookieMiddleware"]
    - path: "tests/relay/test_secure_cookies.py"
      provides: "Tests for SecureCookieMiddleware"
    - path: "tests/relay/test_cors.py"
      provides: "Tests for conditional CORS configuration"
    - path: "tests/relay/test_proxy_headers.py"
      provides: "Tests for proxy header forwarding"
  key_links:
    - from: "relay/app/main.py"
      to: "relay/app/middleware/secure_cookies.py"
      via: "app.add_middleware(SecureCookieMiddleware)"
      pattern: "SecureCookieMiddleware"
    - from: "relay/app/main.py"
      to: "CORSMiddleware"
      via: "Conditional origin list based on RELAY_ENV"
      pattern: "RELAY_ALLOWED_ORIGINS|allow_credentials"
    - from: "relay/cli.py"
      to: "uvicorn.run"
      via: "proxy_headers=True already set by plan 12-01"
      pattern: "proxy_headers"
---

<objective>
Fix three production-blocking security bugs: missing Secure flag on cookies behind HTTPS, wildcard CORS in production, and proxy header forwarding.

Purpose: Without these fixes, the relay deployed on Cloud Run will silently break authentication (cookies rejected by browser), leak credentials via CORS, and misreport its own protocol scheme. These are the security-critical changes needed before any real traffic hits the relay.
Output: SecureCookieMiddleware, conditional CORS configuration, and proxy header verification tests.
</objective>

<execution_context>
@/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3-ROADMAP.md
@.planning/milestones/v1.3-REQUIREMENTS.md
@.planning/milestones/v1.3-phases/12-cloud-run-foundation/12-CONTEXT.md
@.planning/milestones/v1.3-phases/12-cloud-run-foundation/12-RESEARCH.md
@.planning/milestones/v1.3-phases/12-cloud-run-foundation/12-VALIDATION.md
@.planning/milestones/v1.3-phases/12-cloud-run-foundation/12-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Includes plan 12-01 outputs. -->

From relay/app/main.py (after plan 12-01):
```python
def create_relay_app() -> FastAPI:
    # CORSMiddleware with wildcard origins (to be replaced)
    # Includes: landing_router, agent_ws_router, mount_proxy_router, health_router
    # Calls set_registry(MountRegistry())
```

From relay/app/logging.py (created by plan 12-01):
```python
class RelayEnv(str, Enum):
    DEVELOPMENT = "development"
    PRODUCTION = "production"

class CloudJsonFormatter(logging.Formatter): ...
def configure_logging(env: RelayEnv) -> None: ...
```

From relay/cli.py (after plan 12-01):
```python
def main() -> None:
    # Reads PORT from env, RELAY_ENV from env
    # Calls configure_logging(env)
    # uvicorn.run(..., proxy_headers=True, forwarded_allow_ips="*", access_log=False, log_config=None)
```

From relay/app/services/mount_registry.py:
```python
def get_registry() -> MountRegistry  # Global singleton
```

From tests/relay/conftest.py:
```python
@pytest.fixture
def relay_app():        # Fresh app + registry per test
@pytest.fixture
async def relay_client(relay_app):   # AsyncClient backed by ASGI transport
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SecureCookieMiddleware</name>
  <files>relay/app/middleware/__init__.py, relay/app/middleware/secure_cookies.py, tests/relay/test_secure_cookies.py</files>
  <behavior>
    - test_secure_flag_added_when_https: When request has X-Forwarded-Proto: https header, any Set-Cookie header in the response has "; Secure" appended (if not already present)
    - test_no_secure_flag_when_http: When request has X-Forwarded-Proto: http (or no header), Set-Cookie headers are passed through unchanged
    - test_no_duplicate_secure_flag: When response already has "Secure" in Set-Cookie, the middleware does NOT double-stamp it
    - test_non_http_scopes_passthrough: WebSocket scope passes through without modification
    - test_multiple_set_cookie_headers: When response has multiple Set-Cookie headers, all get the Secure flag behind HTTPS
  </behavior>
  <action>
Create `relay/app/middleware/__init__.py` (empty file).

Create `relay/app/middleware/secure_cookies.py` with `SecureCookieMiddleware`:
- Pure ASGI middleware (not Starlette BaseHTTPMiddleware — raw ASGI for correctness with streaming responses).
- `__init__(self, app: ASGIApp) -> None` stores the inner app.
- `__call__(self, scope: Scope, receive: Receive, send: Send) -> None`:
  - If `scope["type"] != "http"`, pass through to inner app unchanged.
  - Extract headers from scope, check if `x-forwarded-proto` is `https`.
  - If not HTTPS, pass through unchanged.
  - If HTTPS, wrap `send` with `send_wrapper` that intercepts `http.response.start` messages. For each header tuple `(key, value)`, if `key.lower() == b"set-cookie"` and `b"secure"` is NOT in `value.lower()`, append `b"; Secure"` to the value.

Write tests FIRST using a minimal FastAPI test app that sets a cookie, then make requests through httpx ASGITransport with and without `X-Forwarded-Proto: https` header. Do NOT use the relay_app fixture — create a standalone test app specifically for this middleware so tests are isolated.

Create the middleware, run tests to GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/relay/test_secure_cookies.py -x -v</automated>
  </verify>
  <done>SecureCookieMiddleware correctly stamps Secure flag on Set-Cookie headers when X-Forwarded-Proto is https. Does not modify cookies when not behind HTTPS. Does not double-stamp. Passes through non-HTTP scopes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conditional CORS, middleware wiring, and proxy header tests</name>
  <files>relay/app/main.py, tests/relay/test_cors.py, tests/relay/test_proxy_headers.py</files>
  <behavior>
    - test_cors_wildcard_in_dev: When RELAY_ENV is not set (default dev), CORS allows any origin
    - test_cors_rejects_unlisted_origin_in_production: When RELAY_ENV=production and RELAY_ALLOWED_ORIGINS="https://example.com", a preflight from "https://evil.com" gets no Access-Control-Allow-Origin header
    - test_cors_allows_listed_origin_in_production: When RELAY_ENV=production and RELAY_ALLOWED_ORIGINS="https://example.com", a preflight from "https://example.com" returns Access-Control-Allow-Origin: https://example.com
    - test_cors_credentials_in_production: Production CORS includes Access-Control-Allow-Credentials: true
    - test_cors_multiple_origins: RELAY_ALLOWED_ORIGINS="https://a.com,https://b.com" allows both
    - test_missing_origins_raises_in_production: RELAY_ENV=production with no RELAY_ALLOWED_ORIGINS raises ValueError
    - test_proxy_headers_scheme: When request includes X-Forwarded-Proto: https, request.url.scheme is https inside the app (verified via a test endpoint)
  </behavior>
  <action>
**Update `relay/app/main.py`:**
- Import `os` and `RelayEnv` from `relay.app.logging`.
- Import `SecureCookieMiddleware` from `relay.app.middleware.secure_cookies`.
- Read `RELAY_ENV` env var, construct `RelayEnv(os.environ.get("RELAY_ENV", "development"))`.
- Replace the current wildcard CORSMiddleware block with conditional logic:
  - If `env == RelayEnv.PRODUCTION`:
    - Read `RELAY_ALLOWED_ORIGINS` from env. If empty or not set, raise `ValueError("RELAY_ALLOWED_ORIGINS must be set when RELAY_ENV=production")`.
    - Parse as comma-separated list, strip whitespace: `[o.strip() for o in raw.split(",")]`.
    - Add CORSMiddleware with `allow_origins=origins`, `allow_credentials=True`, `allow_methods=["*"]`, `allow_headers=["*"]`.
  - If `env == RelayEnv.DEVELOPMENT`:
    - Add CORSMiddleware with `allow_origins=["*"]`, `allow_methods=["*"]`, `allow_headers=["*"]` (no `allow_credentials` — wildcard + credentials is invalid per CORS spec).
- Add `SecureCookieMiddleware` to the app. Add it AFTER CORSMiddleware so it wraps the outermost layer (middleware order in Starlette: last added = outermost). SecureCookieMiddleware should be added first (before CORS) so that CORS middleware is outermost and SecureCookieMiddleware processes response headers closer to the app. Actually: Starlette processes middleware in LIFO order (last added wraps the previous). For SecureCookieMiddleware to see Set-Cookie headers from inner responses, it needs to wrap the app (be closer to the app). CORSMiddleware should be outermost (added last). So: add SecureCookieMiddleware first, then CORSMiddleware.

Wait — the current code adds CORS first, then routers. The correct order: add SecureCookieMiddleware, then CORSMiddleware. In Starlette, `add_middleware` wraps the app, so the last middleware added is the outermost. CORSMiddleware should be outermost to handle preflight before anything else. SecureCookieMiddleware should be inner, so it sees the actual response Set-Cookie headers.

Final middleware addition order in `create_relay_app()`:
1. `application.add_middleware(SecureCookieMiddleware)` — added first, becomes inner
2. `application.add_middleware(CORSMiddleware, ...)` — added second, becomes outer

**Write tests FIRST (RED), then modify main.py (GREEN).**

For `test_cors.py`:
- Use `monkeypatch` to set/unset `RELAY_ENV` and `RELAY_ALLOWED_ORIGINS` env vars.
- Since `create_relay_app()` reads env vars at import time of `main.py`, the tests must call `create_relay_app()` after setting env vars. Create the app inside each test, build an httpx AsyncClient with ASGITransport.
- Test CORS preflight by sending `OPTIONS` request with `Origin` and `Access-Control-Request-Method` headers, checking response headers.
- Test that missing RELAY_ALLOWED_ORIGINS in production raises ValueError (use pytest.raises).

For `test_proxy_headers.py`:
- Create a test that sends a request with `X-Forwarded-Proto: https` header to a simple endpoint and verifies `request.url.scheme` is "https". Use the health endpoint (`GET /health`) for this — add a temporary route or inspect via the health endpoint response. Actually, the simplest approach: create a minimal FastAPI app with a test endpoint that returns `request.url.scheme`, then test with httpx ASGITransport. Note: httpx ASGITransport does NOT process proxy headers the way uvicorn does — uvicorn's proxy header handling is a uvicorn feature, not a Starlette feature. So testing `request.url.scheme` via httpx ASGITransport will NOT validate DEPLOY-06. Instead, verify that `relay/cli.py` passes `proxy_headers=True` and `forwarded_allow_ips="*"` to uvicorn.run by: (a) reading the source of cli.py and asserting the arguments are present, or (b) monkeypatching uvicorn.run and asserting it was called with those kwargs. Use approach (b) — monkeypatch `uvicorn.run` and call `main()`, then assert kwargs include `proxy_headers=True` and `forwarded_allow_ips="*"`.
  </action>
  <verify>
    <automated>uv run pytest tests/relay/test_cors.py tests/relay/test_proxy_headers.py -x -v && uv run pytest tests/relay/ -x</automated>
  </verify>
  <done>CORS in production mode only allows explicitly configured origins with credentials. CORS in dev mode allows wildcard without credentials. Missing RELAY_ALLOWED_ORIGINS in production raises ValueError. SecureCookieMiddleware is wired into the app. uvicorn.run is called with proxy_headers=True and forwarded_allow_ips="*". All relay tests pass.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/relay/ -x -v` -- all relay tests pass (existing + new)
- `RELAY_ENV=production RELAY_ALLOWED_ORIGINS=https://example.com uv run python -c "from relay.app.main import create_relay_app; app = create_relay_app(); print('CORS configured')"` -- no errors
- `uv run python -c "from relay.app.main import create_relay_app; app = create_relay_app(); print('Dev CORS configured')"` -- no errors (dev mode wildcard)
- Docker rebuild: `docker build -t relay-test . --quiet` -- still builds after main.py changes
</verification>

<success_criteria>
- SecureCookieMiddleware stamps Secure flag on cookies behind HTTPS
- Production CORS rejects unlisted origins
- Production CORS allows listed origins with credentials
- Dev CORS allows wildcard origins (no credentials)
- uvicorn receives proxy_headers=True and forwarded_allow_ips="*"
- All relay tests pass (existing + new security tests)
- Docker image still builds
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.3-phases/12-cloud-run-foundation/12-02-SUMMARY.md`
</output>
