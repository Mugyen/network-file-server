---
phase: 13-abuse-prevention
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - relay/config.yaml
  - relay/app/config.py
  - relay/app/rate_limit.py
  - relay/app/main.py
  - relay/app/services/mount_registry.py
  - relay/app/routers/mount_proxy.py
  - relay/templates/rate_limited.html
  - relay/cli.py
  - tests/relay/test_config.py
  - tests/relay/test_rate_limit.py
  - tests/relay/conftest.py
autonomous: true
requirements: [ABUSE-01, ABUSE-02, ABUSE-05]

must_haves:
  truths:
    - "Sending more than 300 proxy requests per minute from the same IP returns HTTP 429 with Retry-After header"
    - "Browser requests hitting 429 see a styled error page with a retry countdown"
    - "Agent/API requests hitting 429 receive JSON with error and retry_after fields"
    - "Rate-limited requests are logged at WARNING level with client IP"
    - "All relay configuration lives in config.yaml with env var overrides"
    - "Existing RELAY_ENV and RELAY_ALLOWED_ORIGINS env vars continue to work"
  artifacts:
    - path: "relay/config.yaml"
      provides: "Development defaults for all relay config"
      contains: "rate_limits"
    - path: "relay/app/config.py"
      provides: "RelayConfig dataclass, load_config(), get_config() singleton"
      exports: ["RelayConfig", "load_config", "get_config", "set_config"]
    - path: "relay/app/rate_limit.py"
      provides: "SlowAPI limiter instance, get_client_ip key function, 429 exception handler"
      exports: ["limiter", "get_client_ip", "rate_limit_exceeded_handler"]
    - path: "relay/templates/rate_limited.html"
      provides: "Styled 429 error page with retry countdown"
      contains: "retry_after"
    - path: "relay/app/services/mount_registry.py"
      provides: "MountRecord with agent_ip, created_at, expires_at fields; count_mounts_by_ip method"
      contains: "agent_ip"
  key_links:
    - from: "relay/app/main.py"
      to: "relay/app/config.py"
      via: "load_config() called in create_relay_app()"
      pattern: "load_config"
    - from: "relay/app/main.py"
      to: "relay/app/rate_limit.py"
      via: "limiter attached to app.state, exception handler registered"
      pattern: "app.state.limiter"
    - from: "relay/app/routers/mount_proxy.py"
      to: "relay/app/rate_limit.py"
      via: "@limiter.limit decorator on proxy_request"
      pattern: "limiter.limit"
    - from: "relay/app/rate_limit.py"
      to: "relay/app/config.py"
      via: "get_config() for rate limit strings"
      pattern: "get_config"
---

<objective>
Create the relay config module, extend MountRecord with abuse-prevention fields, and add SlowAPI rate limiting to proxy requests with styled 429 error responses.

Purpose: Centralizes all relay configuration into a YAML+env-var config module (foundation for this phase and future phases), extends the mount data model for TTL/IP tracking, and protects the proxy endpoint from request flooding.

Output: relay/config.yaml, relay/app/config.py, relay/app/rate_limit.py, extended MountRecord, rate-limited proxy endpoint, styled 429 page, tests.
</objective>

<execution_context>
@/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.3-ROADMAP.md
@.planning/milestones/v1.3-REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.3-phases/13-abuse-prevention/13-CONTEXT.md
@.planning/milestones/v1.3-phases/13-abuse-prevention/13-RESEARCH.md
@.planning/milestones/v1.3-phases/12-cloud-run-foundation/12-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From relay/app/main.py:
```python
def create_relay_app() -> FastAPI:
    # Reads RELAY_ENV, configures CORS, includes routers, initializes registry
    # Currently reads os.environ directly -- will be migrated to config module
```

From relay/app/services/mount_registry.py:
```python
@dataclass
class MountRecord:
    code: str
    connection: "TunnelConnection"
    status: MountStatus

class MountRegistry:
    def register(self, code: str, connection: "TunnelConnection") -> None: ...
    def deregister(self, code: str) -> None: ...
    def get_connection(self, code: str) -> "TunnelConnection": ...
    def has_mount(self, code: str) -> bool: ...

def get_registry() -> MountRegistry: ...
def set_registry(registry: MountRegistry) -> None: ...
```

From relay/app/routers/mount_proxy.py (lines 84-88):
```python
# Client IP extraction pattern -- reuse for SlowAPI key function
client_ip: str = request.headers.get(
    "x-forwarded-for",
    request.client.host if request.client else "unknown",
)
```

From relay/app/logging.py:
```python
class RelayEnv(str, Enum):
    DEVELOPMENT = "development"
    PRODUCTION = "production"
```

From relay/app/routers/landing.py:
```python
_template_dir = Path(__file__).resolve().parent.parent.parent / "templates"
templates = Jinja2Templates(directory=str(_template_dir))
```

From relay/cli.py:
```python
def main() -> None:
    # Reads --host, --port, PORT env var, RELAY_ENV
    # Calls configure_logging(env), then uvicorn.run()
```

From tests/relay/conftest.py:
```python
class MockTunnelConnection: ...

@pytest.fixture
def relay_app():
    app = create_relay_app()
    set_registry(MountRegistry())
    return app

@pytest.fixture
async def relay_client(relay_app):
    transport = httpx.ASGITransport(app=relay_app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        yield client
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config module and MountRecord extensions</name>
  <files>
    relay/config.yaml,
    relay/app/config.py,
    relay/app/services/mount_registry.py,
    relay/app/main.py,
    relay/cli.py,
    tests/relay/test_config.py,
    tests/relay/conftest.py
  </files>
  <behavior>
    - load_config() with default config.yaml returns RelayConfig with env=DEVELOPMENT, mount_reg_rate="5/hour", proxy_request_rate="300/minute", max_ttl_seconds=86400, max_mounts_per_ip=5, ttl_sweep_interval_seconds=45, warning_before_seconds=300
    - load_config() with RELAY_ENV=production env var overrides env field to PRODUCTION
    - load_config() with RELAY_ALLOWED_ORIGINS env var overrides allowed_origins list
    - load_config() with RELAY_MAX_TTL_SECONDS=3600 env var overrides max_ttl_seconds to 3600
    - load_config() validates that production env requires non-empty allowed_origins (raises ValueError)
    - get_config() raises RuntimeError before set_config() is called
    - set_config() / get_config() round-trip returns same instance
    - MountRecord now has agent_ip (str), created_at (float), expires_at (float | None) fields
    - MountRegistry.register() accepts agent_ip, created_at, expires_at parameters
    - MountRegistry.count_mounts_by_ip() counts active (non-EXPIRED) mounts for an IP
    - MountRegistry.count_mounts_by_ip() returns 0 for unknown IP
    - MountRegistry.count_mounts_by_ip() excludes EXPIRED mounts from count
    - create_relay_app() loads config, sets global via set_config(), uses config for CORS/env instead of os.environ
  </behavior>
  <action>
    1. Install slowapi: `uv add slowapi` (needed in this plan for Task 2, install now so lockfile is stable).

    2. Create `relay/config.yaml` with development defaults:
       - env: development
       - allowed_origins: [] (empty means wildcard in dev)
       - rate_limits section: mount_registration: "5/hour", proxy_requests: "300/minute", max_mounts_per_ip: 5
       - ttl section: max_seconds: 86400, sweep_interval_seconds: 45, warning_before_seconds: 300

    3. Create `relay/app/config.py`:
       - Import RelayEnv from relay.app.logging (reuse existing enum, do NOT duplicate).
       - `@dataclass(frozen=True) class RelayConfig` with fields: env (RelayEnv), allowed_origins (list[str]), mount_reg_rate (str), proxy_request_rate (str), max_ttl_seconds (int), max_mounts_per_ip (int), ttl_sweep_interval_seconds (int), warning_before_seconds (int).
       - `load_config(config_path: Path) -> RelayConfig` that reads YAML with yaml.safe_load(), then overrides each field with corresponding env var if present: RELAY_ENV, RELAY_ALLOWED_ORIGINS (comma-separated), RELAY_MOUNT_REG_RATE, RELAY_PROXY_REQUEST_RATE, RELAY_MAX_TTL_SECONDS, RELAY_MAX_MOUNTS_PER_IP, RELAY_TTL_SWEEP_INTERVAL, RELAY_WARNING_BEFORE_SECONDS.
       - Validate eagerly: if env is PRODUCTION and allowed_origins is empty, raise ValueError with clear message (same error as current create_relay_app).
       - Module-level `_config: RelayConfig | None = None` with `get_config() -> RelayConfig` (raises RuntimeError if None) and `set_config(config: RelayConfig) -> None`.

    4. Extend MountRecord in `relay/app/services/mount_registry.py`:
       - Add fields: `agent_ip: str`, `created_at: float`, `expires_at: float | None`.
       - Update `register()` signature to accept `agent_ip: str`, `created_at: float`, `expires_at: float | None` (no defaults per project rules). Update the MountRecord construction.
       - Add `count_mounts_by_ip(self, agent_ip: str) -> int` method that counts mounts where `m.agent_ip == agent_ip and m.status != MountStatus.EXPIRED`.
       - Add `active_mounts(self) -> list[MountRecord]` for the TTL sweep to iterate (returns list copy, not view into dict).

    5. Update `relay/app/main.py` create_relay_app():
       - Accept `config_path: Path | None` parameter. If None, resolve default: `Path(__file__).resolve().parent.parent / "config.yaml"`.
       - Call `load_config(config_path)`, then `set_config(config)`.
       - Replace `os.environ.get("RELAY_ENV", ...)` with `config.env`.
       - Replace CORS origin logic with `config.allowed_origins` and `config.env`.
       - Remove the ValueError for missing RELAY_ALLOWED_ORIGINS (now handled by load_config validation).

    6. Update `relay/cli.py`:
       - Remove `RELAY_ENV` reading from cli.py (now handled by config module inside create_relay_app).
       - Keep configure_logging call but read env from loaded config after app creation, OR call configure_logging with RelayEnv from env var before app creation (since logging must be configured before first log). Decision: keep configure_logging(RelayEnv(os.environ.get("RELAY_ENV", "development"))) in cli.py before uvicorn.run -- logging is CLI concern, not app concern.

    7. Update `tests/relay/conftest.py`:
       - Update relay_app fixture to work with new config module: set env vars or pass config_path.
       - Update any registry.register() calls to include the new agent_ip, created_at, expires_at parameters.
       - Add a fixture for creating a test config.yaml or use env var overrides.

    8. Update existing tests that call registry.register() to pass the new required fields (test_mount_registry.py, test_agent_ws.py, etc.). Use agent_ip="127.0.0.1", created_at=time.monotonic(), expires_at=None for all existing tests.

    IMPORTANT: The RelayEnv enum already exists in relay/app/logging.py. Import it from there in config.py. Do NOT create a duplicate enum.

    Per user decision: "Absorb all existing relay config into the module: RELAY_ENV, RELAY_ALLOWED_ORIGINS, plus all new rate limit settings." The config module replaces scattered os.environ.get() calls in create_relay_app.
  </action>
  <verify>
    <automated>uv run pytest tests/relay/test_config.py tests/relay/test_mount_registry.py -x -v</automated>
  </verify>
  <done>
    - relay/config.yaml exists with development defaults for all rate limit and TTL settings
    - RelayConfig dataclass loads from YAML with env var overrides
    - Production validation rejects empty allowed_origins
    - MountRecord has agent_ip, created_at, expires_at fields
    - count_mounts_by_ip returns correct counts excluding expired
    - create_relay_app uses config module instead of os.environ
    - All existing relay tests still pass (uv run pytest tests/relay/ -x)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SlowAPI proxy rate limiting and 429 error handler</name>
  <files>
    relay/app/rate_limit.py,
    relay/app/main.py,
    relay/app/routers/mount_proxy.py,
    relay/templates/rate_limited.html,
    tests/relay/test_rate_limit.py
  </files>
  <behavior>
    - GET /m/testcode/ returns 200 on first request
    - After 300 requests to /m/testcode/ from same IP within 1 minute, next request returns 429 with Retry-After header
    - 429 response to browser (Accept: text/html) returns styled HTML page with retry countdown
    - 429 response to API client (Accept: application/json) returns JSON with "error" and "retry_after" fields
    - 429 responses are logged at WARNING level with client IP and path
    - Rate limit uses sliding window strategy
    - Rate limit key function extracts IP from X-Forwarded-For header, falling back to request.client.host
  </behavior>
  <action>
    1. Create `relay/app/rate_limit.py`:
       - `get_client_ip(request: Request) -> str` that extracts from X-Forwarded-For (first IP, stripped) or falls back to request.client.host. Raise ValueError if neither available. This replaces the inline pattern in mount_proxy.py:85-88.
       - Create `limiter = Limiter(key_func=get_client_ip, strategy="moving-window", headers_enabled=True)`.
       - `rate_limit_exceeded_handler(request: Request, exc: RateLimitExceeded) -> Response`:
         - Extract retry_after from the response headers SlowAPI attaches, or parse from exc. The Retry-After value is available in `exc.detail` or compute from the rate limit state. Research note: check `exc.detail` format at runtime -- it may be the rate limit description string. Safer approach: use a fixed retry interval derived from the rate string (e.g., 60 for "300/minute", 3600 for "5/hour") or extract from the headers SlowAPI adds.
         - Log at WARNING: "Rate limited: client={ip} path={path}"
         - If Accept header contains "text/html": render rate_limited.html template with retry_after context, status 429, Retry-After header.
         - Otherwise: return JSONResponse with {"error": "Rate limit exceeded", "retry_after": N}, status 429, Retry-After header.

    2. Create `relay/templates/rate_limited.html`:
       - Extend base.html (consistent with not_found.html, offline.html, expired.html pattern).
       - Title: "Rate Limited"
       - Content: heading "Too Many Requests", paragraph "You've made too many requests. Please try again in {{ retry_after }} seconds.", a "Go Back" link/button.
       - Per user decision: "Error page shows the exact retry countdown (driven by Retry-After header value)"

    3. Wire SlowAPI into `relay/app/main.py` create_relay_app():
       - Import limiter from relay.app.rate_limit.
       - Set `application.state.limiter = limiter` (required by SlowAPI).
       - Register exception handler: `application.add_exception_handler(RateLimitExceeded, rate_limit_exceeded_handler)`.
       - Place AFTER middleware setup but BEFORE router inclusion.

    4. Apply rate limit to proxy endpoint in `relay/app/routers/mount_proxy.py`:
       - Import limiter from relay.app.rate_limit and get_config from relay.app.config.
       - Add `@limiter.limit(lambda: get_config().proxy_request_rate)` decorator BELOW the existing `@router.api_route(...)` decorator (per research: route decorator must be outermost).
       - IMPORTANT anti-pattern from research: "@limiter.limit() decorator works on HTTP endpoints but NOT on WebSocket endpoints". Do NOT apply to proxy_websocket.
       - Replace the inline client_ip extraction (lines 85-88) with `get_client_ip(request)` from rate_limit module (DRY per project rules).

    5. Write tests in `tests/relay/test_rate_limit.py`:
       - Test proxy rate limit: send requests to /m/testcode/ with a registered mock connection. After exceeding the configured rate, verify 429 status and Retry-After header presence.
       - Test HTML vs JSON 429 response: send with Accept: text/html vs Accept: application/json, verify content type and body format.
       - Test get_client_ip with X-Forwarded-For header returns first IP.
       - Test get_client_ip without X-Forwarded-For falls back to client.host.
       - Use a tight rate limit for tests (e.g., override config to "2/minute" so tests don't need 300 requests).
       - Important: each test needs a fresh app/limiter to avoid cross-test rate limit state. Use the relay_app fixture pattern.

    NOTE: Mount registration rate limiting (ABUSE-01) is handled in Plan 02 since it requires manual limits library usage on the WebSocket endpoint (not SlowAPI decorator).
  </action>
  <verify>
    <automated>uv run pytest tests/relay/test_rate_limit.py -x -v</automated>
  </verify>
  <done>
    - SlowAPI limiter with moving-window strategy and custom IP key function is active
    - Proxy requests to /m/{code}/* are rate-limited per config (default 300/minute)
    - Browser 429 response renders styled rate_limited.html with retry countdown
    - API/agent 429 response returns JSON with error and retry_after
    - Rate-limited requests logged at WARNING with client IP
    - All existing relay tests still pass (uv run pytest tests/relay/ -x)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `uv run pytest tests/relay/ -x` -- all relay tests pass (existing + new)
2. `uv run pytest` -- full project test suite passes
3. Config module loads defaults from relay/config.yaml
4. Env var overrides work (RELAY_ENV, RELAY_ALLOWED_ORIGINS, rate limit vars)
5. Proxy requests return 429 after exceeding rate limit
</verification>

<success_criteria>
- relay/config.yaml exists with documented defaults
- RelayConfig loads YAML + env var overrides, validates eagerly
- MountRecord has agent_ip, created_at, expires_at fields
- count_mounts_by_ip excludes expired mounts
- SlowAPI rate-limits proxy requests with sliding window
- 429 returns styled HTML (browser) or JSON (API) with Retry-After
- All tests pass including new test_config.py and test_rate_limit.py
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.3-phases/13-abuse-prevention/13-01-SUMMARY.md`
</output>
