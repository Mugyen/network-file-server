---
phase: 06-expiring-share-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/services/share_service.py
  - server/app/models/enums.py
  - server/app/models/schemas.py
  - server/app/routers/share.py
  - server/app/middleware/auth_middleware.py
  - server/app/main.py
  - server/app/cli.py
  - templates/share_download.html
  - templates/share_expired.html
  - templates/share_unavailable.html
  - server/tests/test_share_service.py
  - server/tests/test_share.py
  - server/tests/conftest.py
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-03, SHARE-04, SHARE-05, SHARE-06, SHARE-07]

must_haves:
  truths:
    - "Creating a share link for a file returns a signed token with the chosen TTL"
    - "Visiting /share/{token} renders an HTML download page with file name, size, and download button"
    - "Visiting /share/{token} for an expired token renders a clear expired page"
    - "Visiting /share/{token} for a revoked token renders an expired/invalid page"
    - "Share link pages are accessible without a session cookie (bypass auth middleware)"
    - "GET /api/shares returns all active (non-expired) share links"
    - "DELETE /api/shares/{token} immediately revokes a share link"
    - "Downloading via /share/{token}/download serves the actual file"
  artifacts:
    - path: "server/app/services/share_service.py"
      provides: "ShareLinkService with create_link, validate_token, revoke_link, list_active_links"
      exports: ["ShareLinkService", "get_share_service", "set_share_service", "ShareLinkRecord", "ShareLinkRevokedError", "ShareLinkNotFoundError", "ShareLinkExpiredError"]
    - path: "server/app/models/enums.py"
      provides: "ShareTTL enum with 4 TTL values"
      contains: "class ShareTTL"
    - path: "server/app/models/schemas.py"
      provides: "ShareLinkInfo and CreateShareRequest Pydantic models"
      contains: "class CreateShareRequest"
    - path: "server/app/routers/share.py"
      provides: "Share router with /share/{token}, /share/{token}/download, /api/shares endpoints"
      exports: ["router"]
    - path: "templates/share_download.html"
      provides: "Server-rendered download page (standalone HTML, no React)"
    - path: "templates/share_expired.html"
      provides: "Server-rendered expired link page"
  key_links:
    - from: "server/app/routers/share.py"
      to: "server/app/services/share_service.py"
      via: "get_share_service() singleton"
      pattern: "get_share_service\\(\\)"
    - from: "server/app/routers/share.py"
      to: "server/app/services/file_service.py"
      via: "download_file() for serving actual files"
      pattern: "download_file"
    - from: "server/app/middleware/auth_middleware.py"
      to: "/share routes"
      via: "EXEMPT_PREFIXES tuple"
      pattern: "/share"
    - from: "server/app/main.py"
      to: "server/app/routers/share.py"
      via: "include_router before SPA catch-all"
      pattern: "include_router.*share"
---

<objective>
Build the complete share link backend: service, router, Jinja2 templates, auth bypass, and tests.

Purpose: Establish the full server-side share link infrastructure so that tokens can be created, validated, revoked, and listed, and share link recipients get a standalone HTML download page without needing the React SPA.
Output: ShareLinkService, share router with 5 endpoints, 3 Jinja2 templates, auth middleware bypass, comprehensive tests.
</objective>

<execution_context>
@/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-expiring-share-links/06-CONTEXT.md
@.planning/phases/06-expiring-share-links/06-RESEARCH.md

@server/app/services/auth_service.py
@server/app/main.py
@server/app/middleware/auth_middleware.py
@server/app/models/enums.py
@server/app/models/schemas.py
@server/app/services/file_service.py
@server/app/routers/files.py
@server/app/cli.py
@server/tests/conftest.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From server/app/services/auth_service.py:
```python
class AuthTokenService:
    def __init__(self, secret_key: str) -> None: ...
    def create_token(self) -> str: ...
    def validate_token(self, token: str) -> bool: ...

_token_service: AuthTokenService | None = None
def get_token_service() -> AuthTokenService: ...
def set_token_service(service: AuthTokenService) -> None: ...
```

From server/app/middleware/auth_middleware.py:
```python
EXEMPT_PREFIXES = ("/api/auth/login", "/api/server-info", "/assets")
```

From server/app/models/enums.py (existing enums):
```python
class FileType(str, Enum): ...
class ConflictResolution(str, Enum): ...
class WSMessageType(str, Enum): ...
class ToastType(str, Enum): ...
class RequestStatus(str, Enum): ...
```

From server/app/main.py (router registration pattern):
```python
application.include_router(auth_router)
application.include_router(clipboard_router)
# ... more routers ...
# SPA catch-all at the very end: @application.get("/{path:path}")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShareLinkService, enums, schemas, and unit tests</name>
  <files>server/app/services/share_service.py, server/app/models/enums.py, server/app/models/schemas.py, server/tests/test_share_service.py</files>
  <behavior>
    - create_link(file_path, ttl) returns a non-empty string token and registers it in active links
    - validate_token(valid_token) returns the original file_path string
    - validate_token(revoked_token) raises ShareLinkRevokedError
    - validate_token(expired_token) raises ShareLinkExpiredError (use max_age=1 and sleep(2) or mock time)
    - validate_token(tampered_token) raises BadSignature-based error
    - revoke_link(valid_token) removes it from active links; subsequent validate raises revoked error
    - revoke_link(unknown_token) raises ShareLinkNotFoundError
    - list_active_links() returns all non-expired links, filtering out naturally expired ones during listing
    - ShareTTL enum has exactly 4 members: FIFTEEN_MINUTES=900, ONE_HOUR=3600, SIX_HOURS=21600, TWENTY_FOUR_HOURS=86400
    - get_share_service() raises RuntimeError when not initialized
    - set_share_service() then get_share_service() returns the service
  </behavior>
  <action>
    1. Add `ShareTTL(int, Enum)` to `server/app/models/enums.py` with 4 values (FIFTEEN_MINUTES=900, ONE_HOUR=3600, SIX_HOURS=21600, TWENTY_FOUR_HOURS=86400).

    2. Add to `server/app/models/schemas.py`:
       - `CreateShareRequest(BaseModel)` with fields `file_path: str` and `ttl: ShareTTL`
       - `ShareLinkInfo(BaseModel)` with fields `token: str`, `file_path: str`, `file_name: str`, `created_at: str`, `expires_at: str`, `ttl_seconds: int`, `share_url: str`

    3. Create `server/app/services/share_service.py` with:
       - `ShareLinkRecord` dataclass: token, file_path, created_at (datetime), ttl_seconds (int)
       - `ShareLinkRevokedError(Exception)`, `ShareLinkNotFoundError(Exception)`, `ShareLinkExpiredError(Exception)` -- all carrying the token string
       - `ShareLinkService` class:
         - `__init__(self, secret_key: str)` -- creates URLSafeTimedSerializer, initializes `_active_links: dict[str, ShareLinkRecord]`
         - `SALT = "share-link"` class constant (different from auth service)
         - `create_link(self, file_path: str, ttl: ShareTTL) -> str` -- dumps {path: file_path} with salt, registers in _active_links, returns token
         - `validate_token(self, token: str) -> str` -- checks registry first (raises ShareLinkRevokedError if missing), then loads with max_age=record.ttl_seconds (catches SignatureExpired -> raises ShareLinkExpiredError, cleans up expired entry; catches BadSignature -> raises BadSignature). Returns file_path string.
         - `revoke_link(self, token: str) -> None` -- removes from _active_links, raises ShareLinkNotFoundError if not present
         - `list_active_links(self) -> list[ShareLinkRecord]` -- iterates _active_links, tries serializer.loads(max_age=record.ttl_seconds) for each, removes expired entries, returns non-expired list
       - Module-level singleton: `_share_service`, `get_share_service()`, `set_share_service()`

    4. Create `server/tests/test_share_service.py` with unit tests covering all behaviors above. Use `time.sleep(2)` with `max_age=1` for expiry tests, or mock `time.time` for speed. Import ShareTTL from enums.
  </action>
  <verify>
    <automated>cd /Users/rahul/Projects/network-file-server && uv run pytest server/tests/test_share_service.py -x -v</automated>
  </verify>
  <done>ShareLinkService passes all unit tests: create, validate, revoke, list, expiry cleanup, singleton access. ShareTTL enum has 4 values. Pydantic schemas exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Share router, Jinja2 templates, auth bypass, app wiring, and integration tests</name>
  <files>server/app/routers/share.py, templates/share_download.html, templates/share_expired.html, templates/share_unavailable.html, server/app/middleware/auth_middleware.py, server/app/main.py, server/app/cli.py, server/tests/test_share.py, server/tests/conftest.py</files>
  <behavior>
    - POST /api/shares with {file_path, ttl} returns 201 with {token, share_url, ...}
    - POST /api/shares with nonexistent file returns 404
    - GET /api/shares returns list of active share links as JSON
    - DELETE /api/shares/{token} returns 204 on success
    - DELETE /api/shares/{token} with unknown token returns 404
    - GET /share/{token} returns HTML page with file name, size, download button (Content-Type: text/html)
    - GET /share/{token} for expired token returns HTML expired page
    - GET /share/{token} for revoked token returns HTML expired page
    - GET /share/{token}/download returns the actual file with correct Content-Disposition
    - GET /share/{token}/download for expired token returns HTML expired page
    - GET /share/{token} on password-protected server works without session cookie (SHARE-05)
    - GET /share/{token} for a file that was deleted after link creation shows "file unavailable" page
  </behavior>
  <action>
    1. Create `server/app/routers/share.py`:
       - Use `APIRouter()` (no prefix -- routes include their own paths)
       - Set up `Jinja2Templates` with directory resolved via `Path(__file__).resolve().parent.parent.parent.parent / "templates"` (project root / templates)
       - `POST /api/shares` -- validates file exists via resolve_safe_path, calls share_service.create_link, returns 201 with ShareLinkInfo JSON. Build share_url using `request.base_url`.
       - `GET /api/shares` -- calls share_service.list_active_links(), returns list of ShareLinkInfo JSON with computed file_name, share_url, created_at, expires_at
       - `DELETE /api/shares/{token}` -- calls share_service.revoke_link(), returns 204. Catches ShareLinkNotFoundError -> 404.
       - `GET /share/{token}` -- validates token, resolves file info (name, size via os.path.getsize), returns TemplateResponse("share_download.html", {...}). On ShareLinkExpiredError/ShareLinkRevokedError -> TemplateResponse("share_expired.html"). On FileNotFoundError (file deleted) -> TemplateResponse("share_unavailable.html").
       - `GET /share/{token}/download` -- validates token, calls download_file() from file_service, returns FileResponse. On expired/revoked -> TemplateResponse expired page.

    2. Create 3 Jinja2 templates in `templates/`:
       - `share_download.html`: Standalone HTML page (no React). Clean centered layout. Shows "Network File Server" title, file name, formatted file size, and a prominent "Download" button linking to `/share/{{token}}/download`. Include inline CSS for dark mode support via prefers-color-scheme media query. Responsive layout.
       - `share_expired.html`: Standalone HTML page with "This link has expired" message, friendly tone. Same styling approach as download page.
       - `share_unavailable.html`: "File no longer available" message for deleted/moved files.

    3. Update `server/app/middleware/auth_middleware.py`: Add `"/share"` to `EXEMPT_PREFIXES` tuple so share routes bypass auth.

    4. Update `server/app/main.py`:
       - Import share router: `from server.app.routers.share import router as share_router`
       - Add `application.include_router(share_router)` BEFORE the SPA catch-all block (after existing router includes)
       - In the try/except block for auth middleware, also initialize ShareLinkService: import get_server_config, create ShareLinkService with the same secret_key pattern used by AuthTokenService, call set_share_service(). If no password config, generate a random secret key for shares using `secrets.token_hex(32)`.

    5. Update `server/app/cli.py`: Initialize ShareLinkService alongside AuthTokenService during startup. Use the same secret key (from AuthTokenService if password is set, or generate one if not). Call set_share_service().

    6. Update `server/tests/conftest.py`: Add a `share_app` or `configured_app_with_shares` fixture that initializes ShareLinkService alongside existing fixtures. Ensure the fixture creates a test file in the shared folder for share link tests.

    7. Create `server/tests/test_share.py` with integration tests covering all behaviors above. Use httpx.AsyncClient with the test app. For expiry tests, create a link with FIFTEEN_MINUTES TTL and mock time or use a custom short-TTL for testing.
  </action>
  <verify>
    <automated>cd /Users/rahul/Projects/network-file-server && uv run pytest server/tests/test_share.py server/tests/test_share_service.py -x -v && uv run pytest server/tests/ -x</automated>
  </verify>
  <done>All share router endpoints work correctly. Download page renders as standalone HTML. Expired/revoked links show appropriate error pages. Auth bypass confirmed for password-protected servers. Full test suite passes with zero regressions.</done>
</task>

</tasks>

<verification>
- `uv run pytest server/tests/test_share_service.py -x -v` -- all share service unit tests pass
- `uv run pytest server/tests/test_share.py -x -v` -- all share router integration tests pass
- `uv run pytest server/tests/ -x` -- full test suite passes with zero regressions
- Manual: `curl http://localhost:8000/api/shares` returns empty list
- Manual: Create a link via POST, visit the /share/{token} URL in browser -- see HTML download page
</verification>

<success_criteria>
- ShareLinkService creates, validates, revokes, lists, and auto-cleans expired tokens
- Share router serves standalone HTML download/expired/unavailable pages via Jinja2
- Share routes bypass auth middleware (SHARE-05)
- All 7 SHARE requirements have backend support
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-expiring-share-links/06-01-SUMMARY.md`
</output>
