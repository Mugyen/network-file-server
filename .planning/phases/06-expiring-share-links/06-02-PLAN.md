---
phase: 06-expiring-share-links
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - client/src/api/shares.ts
  - client/src/components/ShareDialog.tsx
  - client/src/components/FileRow.tsx
  - client/src/components/ShareLinksPanel.tsx
  - client/src/components/App.tsx
autonomous: true
requirements: [SHARE-01, SHARE-02, SHARE-06, SHARE-07]

must_haves:
  truths:
    - "User sees a Share button on file rows (not directory rows) that opens a dialog"
    - "Share dialog presents a TTL dropdown with exactly 4 options and a Create button"
    - "After creating a share link, user sees the URL and can copy it with one click"
    - "User can open a share links panel showing all active links with file name, TTL, expiry, and share URL"
    - "User can revoke any active link from the panel, immediately removing it from the list"
  artifacts:
    - path: "client/src/api/shares.ts"
      provides: "API client functions for share endpoints"
      exports: ["createShareLink", "listShareLinks", "revokeShareLink"]
    - path: "client/src/components/ShareDialog.tsx"
      provides: "Modal dialog for creating share links with TTL selection and URL copy"
    - path: "client/src/components/ShareLinksPanel.tsx"
      provides: "Panel listing active share links with revoke action"
    - path: "client/src/components/FileRow.tsx"
      provides: "Updated with Share button for files"
  key_links:
    - from: "client/src/components/ShareDialog.tsx"
      to: "client/src/api/shares.ts"
      via: "createShareLink() call on form submit"
      pattern: "createShareLink"
    - from: "client/src/components/ShareLinksPanel.tsx"
      to: "client/src/api/shares.ts"
      via: "listShareLinks() and revokeShareLink() calls"
      pattern: "listShareLinks|revokeShareLink"
    - from: "client/src/components/FileRow.tsx"
      to: "client/src/components/ShareDialog.tsx"
      via: "State toggle opening ShareDialog on Share button click"
      pattern: "ShareDialog"
---

<objective>
Build the React frontend for share link creation and management.

Purpose: Give users the UI to create share links (with TTL selection and clipboard copy) and manage active links (list and revoke). This completes the user-facing share feature.
Output: ShareDialog, ShareLinksPanel, shares API client, FileRow Share button, panel toggle in App.
</objective>

<execution_context>
@/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-expiring-share-links/06-CONTEXT.md
@.planning/phases/06-expiring-share-links/06-RESEARCH.md
@.planning/phases/06-expiring-share-links/06-01-SUMMARY.md

@client/src/components/FileRow.tsx
@client/src/components/App.tsx
@client/src/components/ScratchpadPanel.tsx
@client/src/api/client.ts
@client/src/api/files.ts

<interfaces>
<!-- Backend API contracts from Plan 01 -->

POST /api/shares
Request: { "file_path": "photos/cat.jpg", "ttl": 900 }
Response 201: { "token": "...", "file_path": "photos/cat.jpg", "file_name": "cat.jpg", "created_at": "2026-03-10T12:00:00Z", "expires_at": "2026-03-10T12:15:00Z", "ttl_seconds": 900, "share_url": "http://192.168.1.5:8080/share/..." }

GET /api/shares
Response 200: [ { "token": "...", "file_path": "...", "file_name": "...", "created_at": "...", "expires_at": "...", "ttl_seconds": 900, "share_url": "..." }, ... ]

DELETE /api/shares/{token}
Response 204 (success) or 404 (not found)

From client/src/api/client.ts:
```typescript
export async function apiFetch<T>(url: string, options?: RequestInit): Promise<T>
export async function apiPost<T>(url: string, body: unknown): Promise<T>
export async function apiDelete(url: string): Promise<void>
```

From client/src/components/FileRow.tsx (action buttons pattern):
```typescript
// Existing action buttons: Download, Pencil (rename), Trash2 (delete)
// Share button goes alongside Download for files only (!isDirectory)
// readOnly prop hides write actions but Share should still be visible
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shares API client and ShareDialog component</name>
  <files>client/src/api/shares.ts, client/src/components/ShareDialog.tsx, client/src/components/FileRow.tsx</files>
  <action>
    1. Create `client/src/api/shares.ts`:
       - `ShareLinkInfo` interface matching backend schema: token, file_path, file_name, created_at, expires_at, ttl_seconds, share_url
       - `ShareTTL` enum-like const: `{ FIFTEEN_MINUTES: 900, ONE_HOUR: 3600, SIX_HOURS: 21600, TWENTY_FOUR_HOURS: 86400 } as const`
       - `TTL_LABELS: Record<number, string>` mapping values to display labels ("15 minutes", "1 hour", "6 hours", "24 hours")
       - `createShareLink(filePath: string, ttl: number): Promise<ShareLinkInfo>` -- POST /api/shares
       - `listShareLinks(): Promise<ShareLinkInfo[]>` -- GET /api/shares
       - `revokeShareLink(token: string): Promise<void>` -- DELETE /api/shares/{encodeURIComponent(token)}

    2. Create `client/src/components/ShareDialog.tsx`:
       - Props: `filePath: string`, `fileName: string`, `onClose: () => void`
       - State: selectedTTL (default: ONE_HOUR), shareUrl (null initially), loading, copied
       - Two-phase UI:
         Phase 1 (before creation): TTL dropdown with 4 options from TTL_LABELS, "Create Share Link" button
         Phase 2 (after creation): Read-only input showing the share URL, "Copy" button. On copy success, show "Copied!" feedback for 2 seconds.
       - Modal overlay with centered card (similar pattern to ConfirmDialog/ConflictDialog). Title: "Share {fileName}".
       - Use `navigator.clipboard.writeText()` for copy. Handle clipboard API failure gracefully (select the URL text as fallback).
       - Close on Escape key and overlay click.
       - Dark mode support with dark: Tailwind classes.

    3. Update `client/src/components/FileRow.tsx`:
       - Import `Share2` from lucide-react alongside existing icons
       - Add state: `isSharing: boolean` (default false)
       - Add Share button next to Download button, visible for files only (`!isDirectory`). Place it BEFORE the Download button for visual grouping. The Share button should be visible even in readOnly mode (sharing a file is not a write operation).
       - Button: `<button onClick={() => setIsSharing(true)} title="Share"><Share2 className="h-4 w-4" /></button>` with same hover styling as existing action buttons (text-gray-400 hover:text-blue-600).
       - Render `{isSharing && <ShareDialog filePath={fullPath} fileName={file.name} onClose={() => setIsSharing(false)} />}` at the end of the component.
  </action>
  <verify>
    <automated>cd /Users/rahul/Projects/network-file-server && cd client && npx tsc --noEmit</automated>
  </verify>
  <done>Share button appears on file rows (not directories). Clicking it opens ShareDialog with TTL picker. Creating a link shows the URL. Copy button works. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: ShareLinksPanel and App wiring</name>
  <files>client/src/components/ShareLinksPanel.tsx, client/src/components/App.tsx</files>
  <action>
    1. Create `client/src/components/ShareLinksPanel.tsx`:
       - Slide-out panel following the same pattern as ScratchpadPanel (fixed overlay, translate-x transition, right-side panel).
       - Props: `isOpen: boolean`, `onClose: () => void`
       - On open, fetch active links via `listShareLinks()`. Store in local state. Refetch on open.
       - Display each link as a card/row showing: file_name, created_at (formatted), expires_at (formatted), remaining TTL as relative time ("expires in 45min" using a simple calculation from expires_at), share_url (truncated with copy button).
       - Revoke button per link: calls `revokeShareLink(token)`, removes from local state on success. Use a confirm pattern or just immediate revoke (per user decision: no bulk revoke needed).
       - Empty state: "No active share links" message.
       - Dark mode support.
       - Show link count in header.

    2. Update `client/src/components/App.tsx`:
       - Import ShareLinksPanel
       - Add state: `showShareLinks: boolean` (default false)
       - Add a button in the header/toolbar area (near the existing ScratchpadPanel toggle or ThemeToggle) with `Link` or `Share2` icon from lucide-react and text label or tooltip "Share Links". Only show this button when NOT in receive mode (receive mode has no file listing to share from).
       - Render `<ShareLinksPanel isOpen={showShareLinks} onClose={() => setShowShareLinks(false)} />` alongside existing panels.
  </action>
  <verify>
    <automated>cd /Users/rahul/Projects/network-file-server && cd client && npx tsc --noEmit && cd .. && uv run pytest server/tests/ -x</automated>
  </verify>
  <done>Share links panel opens from header button. Shows active links with file name, TTL, expiry, URL. Revoke removes link immediately. Full backend test suite still passes. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `cd client && npx tsc --noEmit` -- TypeScript compiles without errors
- `uv run pytest server/tests/ -x` -- full backend test suite passes
- Manual: Click Share on a file row -> see ShareDialog with TTL dropdown
- Manual: Create a link -> see URL, copy it -> paste shows correct URL
- Manual: Open Share Links panel -> see the created link with expiry info
- Manual: Revoke a link -> disappears from panel, visiting URL shows expired page
</verification>

<success_criteria>
- Share button visible on file rows, not directory rows
- ShareDialog shows TTL picker with 4 options and creates links
- Copy button copies share URL to clipboard
- ShareLinksPanel lists all active links with revoke capability
- No TypeScript errors, no backend test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-expiring-share-links/06-02-SUMMARY.md`
</output>
