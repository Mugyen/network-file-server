---
phase: 08-tunnel-protocol
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tunnel/__init__.py
  - tunnel/constants.py
  - tunnel/enums.py
  - tunnel/exceptions.py
  - tunnel/frames.py
  - tunnel/protocol.py
  - tests/tunnel/__init__.py
  - tests/tunnel/conftest.py
  - tests/tunnel/test_frames.py
  - pyproject.toml
autonomous: true
requirements:
  - TUNL-01
  - TUNL-04

must_haves:
  truths:
    - "Binary frames with 21-byte headers serialize and deserialize correctly for all FrameType values"
    - "Oversized payloads (>64 KB) are rejected with FrameTooLargeError"
    - "Undersized frames (<21 bytes) are rejected with TunnelError"
    - "Payload length mismatch between header and actual data is rejected"
    - "WebSocketProtocol defines the framework-agnostic interface for send/receive"
    - "Control messages use JSON text frames, data uses binary frames — distinguished by WebSocket frame type"
  artifacts:
    - path: "tunnel/constants.py"
      provides: "Protocol constants"
      contains: "HEADER_FORMAT"
    - path: "tunnel/enums.py"
      provides: "FrameType enum with all 7 frame types"
      contains: "class FrameType"
    - path: "tunnel/exceptions.py"
      provides: "Typed exception hierarchy"
      contains: "FrameTooLargeError"
    - path: "tunnel/frames.py"
      provides: "serialize_frame and deserialize_frame functions"
      exports: ["serialize_frame", "deserialize_frame"]
    - path: "tunnel/protocol.py"
      provides: "WebSocketProtocol typing.Protocol"
      contains: "class WebSocketProtocol"
    - path: "tests/tunnel/test_frames.py"
      provides: "Comprehensive frame serialization tests"
      min_lines: 50
    - path: "tests/tunnel/conftest.py"
      provides: "MockWebSocket fixture"
      contains: "class MockWebSocket"
  key_links:
    - from: "tunnel/frames.py"
      to: "tunnel/enums.py"
      via: "import FrameType"
      pattern: "from tunnel\\.enums import FrameType"
    - from: "tunnel/frames.py"
      to: "tunnel/constants.py"
      via: "import HEADER_FORMAT, HEADER_SIZE, MAX_PAYLOAD_BYTES"
      pattern: "from tunnel\\.constants import"
    - from: "tests/tunnel/test_frames.py"
      to: "tunnel/frames.py"
      via: "import serialize_frame, deserialize_frame"
      pattern: "from tunnel\\.frames import"
---

<objective>
Create the tunnel protocol primitives: binary frame serialization/deserialization, frame type enum, protocol constants, typed exceptions, and the framework-agnostic WebSocket protocol interface. This is the foundation layer that TunnelConnection (Plan 02) builds on.

Purpose: Establish the wire format and type contracts so Plan 02 can implement multiplexing against well-tested primitives.
Output: Working `tunnel/` package with frame round-trip tests passing, plus MockWebSocket test infrastructure.
</objective>

<execution_context>
@/Users/rahul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rahul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tunnel-protocol/08-CONTEXT.md
@.planning/phases/08-tunnel-protocol/08-RESEARCH.md
@.planning/phases/08-tunnel-protocol/08-VALIDATION.md

<interfaces>
<!-- Existing enum pattern from the project -->
From server/app/models/enums.py:
```python
class ShareTTL(int, Enum):
    """Time-to-live options for share links, in seconds."""
    FIFTEEN_MINUTES = 900
    ONE_HOUR = 3600
```

<!-- pyproject.toml test config (line 41-42) -->
testpaths = ["server/tests"]
asyncio_mode = "auto"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frame primitives with TDD (constants, enums, exceptions, frames, protocol)</name>
  <files>
    tunnel/__init__.py,
    tunnel/constants.py,
    tunnel/enums.py,
    tunnel/exceptions.py,
    tunnel/frames.py,
    tunnel/protocol.py,
    tests/tunnel/__init__.py,
    tests/tunnel/conftest.py,
    tests/tunnel/test_frames.py,
    pyproject.toml
  </files>
  <behavior>
    - serialize_frame(FrameType.DATA, uuid, b"hello") produces 21-byte header + b"hello" (26 bytes total)
    - deserialize_frame on serialized output returns identical (frame_type, request_id, payload)
    - Round-trip works for all 7 FrameType values (OPEN, DATA, CLOSE, CANCEL, ERROR, PING, PONG)
    - serialize_frame with payload > 65536 bytes raises FrameTooLargeError
    - deserialize_frame with data shorter than 21 bytes raises TunnelError
    - deserialize_frame with payload_length in header not matching actual payload raises TunnelError
    - serialize_frame with empty payload (b"") works correctly (header only, payload_length=0)
    - FrameType values are sequential hex bytes: OPEN=0x01, DATA=0x02, CLOSE=0x03, CANCEL=0x04, ERROR=0x05, PING=0x06, PONG=0x07
    - HEADER_SIZE constant equals 21 (struct.calcsize of format string)
    - MAX_PAYLOAD_BYTES equals 65536 (64 KB)
    - Invalid frame type byte (e.g., 0xFF) in deserialization raises ValueError
    - UUID bytes are preserved exactly through round-trip (big-endian, 16 bytes in header)
  </behavior>
  <action>
    TDD RED-GREEN-REFACTOR cycle:

    **RED phase:**
    1. Create `tunnel/__init__.py` (empty initially).
    2. Create `tunnel/constants.py` with all protocol constants:
       - HEADER_FORMAT = ">B16sI" (big-endian: 1B type, 16B UUID, 4B uint32 length)
       - HEADER_SIZE = 21 (use struct.calcsize)
       - MAX_PAYLOAD_BYTES = 65536 (64 KB)
       - MAX_STREAMS = 100
       - QUEUE_DEPTH = 64
       - HEARTBEAT_INTERVAL_S = 15
       - HEARTBEAT_MISSED_LIMIT = 3
       - FIRST_BYTE_TIMEOUT_S = 30
    3. Create `tunnel/enums.py` with FrameType(int, Enum) — OPEN=0x01, DATA=0x02, CLOSE=0x03, CANCEL=0x04, ERROR=0x05, PING=0x06, PONG=0x07. Follow the project's enum pattern from server/app/models/enums.py.
    4. Create `tunnel/exceptions.py` with exception hierarchy:
       - TunnelError(Exception) — base
       - FrameTooLargeError(TunnelError)
       - StreamLimitError(TunnelError)
       - FirstByteTimeoutError(TunnelError)
       - StreamNotFoundError(TunnelError)
    5. Create `tunnel/protocol.py` with WebSocketProtocol(Protocol) — runtime_checkable, methods: send_bytes, send_text, receive_bytes, receive_text. Also add a receive() method returning dict with "bytes" or "text" key (for the unified receive loop in Plan 02).
    6. Create `tests/tunnel/__init__.py` (empty).
    7. Create `tests/tunnel/conftest.py` with MockWebSocket class implementing WebSocketProtocol. Use asyncio.Queue for inbound frames (both binary and text). Track sent frames in lists. Add a unified receive() that returns from either queue. Add feed_binary() and feed_text() helpers.
    8. Create `tests/tunnel/test_frames.py` with all tests from <behavior> — these should FAIL because frames.py doesn't exist yet.
    9. Update `pyproject.toml` testpaths from `["server/tests"]` to `["server/tests", "tests"]`.
    10. Run tests — confirm RED (all fail with ImportError since frames.py doesn't exist).

    **GREEN phase:**
    11. Create `tunnel/frames.py` with serialize_frame() and deserialize_frame():
        - serialize_frame(frame_type: FrameType, request_id: uuid.UUID, payload: bytes) -> bytes
        - deserialize_frame(data: bytes) -> tuple[FrameType, uuid.UUID, bytes]
        - Validate payload size, header size, payload length match
        - Use struct.pack/unpack with HEADER_FORMAT
        - Strict types, no optionals, raise exceptions on invalid input
    12. Run tests — confirm GREEN (all pass).

    **REFACTOR phase (if needed):**
    13. Update `tunnel/__init__.py` to re-export public API: FrameType, TunnelFrame (if used), serialize_frame, deserialize_frame, WebSocketProtocol, all exception types, all constants.
    14. Run tests — confirm still GREEN.

    Important: Do NOT use default parameters. Do NOT import FastAPI anywhere in the tunnel module. Use strict type annotations everywhere.
  </action>
  <verify>
    <automated>uv run pytest tests/tunnel/test_frames.py -x -v</automated>
  </verify>
  <done>
    All 12+ frame serialization tests pass. tunnel/ package exists with constants, enums, exceptions, frames, protocol modules. pyproject.toml testpaths updated. MockWebSocket available in conftest.py for Plan 02.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/tunnel/test_frames.py -x -v` — all frame tests green
- `uv run pytest server/tests/ -x -q` — existing tests still pass (no regressions)
- `python -c "from tunnel import serialize_frame, deserialize_frame, FrameType, WebSocketProtocol"` — public API importable
- `python -c "from tunnel.constants import HEADER_SIZE; assert HEADER_SIZE == 21"` — constants correct
</verification>

<success_criteria>
- Frame serialize/deserialize round-trip works for all 7 FrameType values
- Oversized, undersized, and mismatched payloads raise correct typed exceptions
- WebSocketProtocol is defined and runtime_checkable
- MockWebSocket is available as test infrastructure for Plan 02
- Existing server tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/08-tunnel-protocol/08-01-SUMMARY.md`
</output>
