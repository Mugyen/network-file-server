# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# macOS
.DS_Store

# Runtime data (relay/server local state)
.wfs_data/

# Local test fixtures — large binaries, not for version control
test-files/

# Relay service env (contains RELAY_SESSION_SECRET) — never commit
.relay.env
